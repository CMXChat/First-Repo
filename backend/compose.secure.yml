services:
  app:
    command:
      - sh
      - -c
      - >-
        alembic upgrade head &&
        exec uvicorn app.hardened:app
        --host 0.0.0.0
        --port 8000
        --proxy-headers
        --forwarded-allow-ips=*
