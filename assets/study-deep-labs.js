(()=>{
  if(window.__studyDeepLabsLoaded)return;window.__studyDeepLabsLoaded=true;
  const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];
  const path=(location.pathname.replace(/\/+$/,'')||'/')+'/';
  if(!q('link[href^="/assets/study-deep-labs.css"]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/assets/study-deep-labs.css?v=20260812-1';document.head.appendChild(l)}
  const mount=(host,html)=>{if(!host)return null;const wrap=document.createElement('div');wrap.innerHTML=html.trim();const el=wrap.firstElementChild;host.appendChild(el);return el};
  const animateNodes=(nodes,caption,copy,ms=760)=>{let i=0,t;const show=()=>{nodes.forEach((n,x)=>{n.classList.toggle('active',x===i);n.classList.toggle('done',x<i)});if(caption&&copy[i])caption.innerHTML=`<strong>${copy[i][0]}:</strong> ${copy[i][1]}`};show();t=setInterval(()=>{i++;if(i>=nodes.length){clearInterval(t);nodes.forEach(n=>{n.classList.remove('active');n.classList.add('done')});return}show()},ms);return()=>clearInterval(t)};

  if(path==='/study/'){
    const react=q('#react');if(react&&!q('.react-playground',react)){const lab=mount(react,`<div class="study-deep-lab"><div class="study-deep-head"><div><span class="study-deep-tag">props + state · visual first</span><h3>Same component. Different data. Different screen memory.</h3></div><p>Props are stuff handed into a component. State is what the frontend is remembering right now. The card shell can stay the same while both change.</p></div><div class="react-playground"><div class="react-control-panel"><label>Prop: city<select data-react-city><option>New York</option><option>London</option><option>Auckland</option></select></label><label>State: selected Space<select data-react-space><option>Personal</option><option>Family</option><option>Business</option></select></label><div class="react-state-readout">prop.city = <b data-prop-read>New York</b><br>state.selectedSpace = <b data-state-read>Personal</b></div><div class="study-deep-actions"><button data-react-random type="button">🎲 Mix it up</button></div></div><div class="react-screen"><article class="react-demo-card"><span class="space-pill" data-space-pill>Personal Space</span><h4>WeatherCard</h4><p data-city-copy>New York data was handed into this reusable card.</p><div class="react-line"><span>Component shell</span><b>same</b></div><div class="react-line"><span>Data + screen state</span><b>changed</b></div></article></div></div></div>`);const city=q('[data-react-city]',lab),space=q('[data-react-space]',lab),card=q('.react-demo-card',lab);const draw=()=>{q('[data-prop-read]',lab).textContent=city.value;q('[data-state-read]',lab).textContent=space.value;q('[data-space-pill]',lab).textContent=`${space.value} Space`;q('[data-city-copy]',lab).textContent=`${city.value} data was handed into this reusable card.`;card.classList.remove('flash');void card.offsetWidth;card.classList.add('flash')};city.addEventListener('change',draw);space.addEventListener('change',draw);q('[data-react-random]',lab).addEventListener('click',()=>{city.selectedIndex=Math.floor(Math.random()*city.options.length);space.selectedIndex=Math.floor(Math.random()*space.options.length);draw()})}

    const request=q('#request');if(request&&!q('.request-mail-stage',request)){const lab=mount(request,`<div class="study-deep-lab"><div class="study-deep-head"><div><span class="study-deep-tag">open the damn request 😭</span><h3>HTTP request = an envelope with labeled compartments</h3></div><p>Method says what kind of action. Path says which backend door. Headers carry extra information. Body can carry data. Auth proof may travel with protected requests.</p></div><div class="request-mail-stage"><div class="request-envelope"><div class="study-deep-actions"><button class="active" data-mail-mode="post" type="button">POST /briefings</button><button data-mail-mode="get" type="button">GET /briefings/42</button></div><div class="request-envelope-shell" role="button" tabindex="0"><strong>✉️ Tap to open request</strong><small>yes, networking has mail now</small></div><div class="request-envelope-parts"></div></div><div><div class="request-road"><article class="request-road-node"><span>01</span><strong>React</strong><small>build request</small></article><article class="request-road-node"><span>02</span><strong>HTTPS</strong><small>encrypted trip</small></article><article class="request-road-node"><span>03</span><strong>API route</strong><small>matching door</small></article><article class="request-road-node"><span>04</span><strong>Python</strong><small>do the work</small></article><article class="request-road-node"><span>05</span><strong>Response</strong><small>answer frontend</small></article></div><div class="request-road-caption"><strong>Start:</strong> open the envelope first, then animate the trip.</div><div class="study-deep-actions"><button class="primary" data-mail-run type="button">▶ Send request</button></div></div></div></div>`);let mode='post',stop;const parts=q('.request-envelope-parts',lab);const render=()=>{const data=mode==='post'?[['Method','POST','please create/change something'],['Path','/briefings','which backend door'],['Headers','Content-Type + login proof','extra request info'],['Body','{"title":"Morning Brief"}','data being sent']]:[['Method','GET','please give me something'],['Path','/briefings/42','which backend door'],['Headers','login proof','extra request info'],['Body','usually none','GET often asks without a body']];parts.innerHTML=data.map(x=>`<div class="request-part"><span>${x[0]}</span><strong>${x[1]}</strong><small>${x[2]}</small></div>`).join('')};render();const env=q('.request-envelope',lab),shell=q('.request-envelope-shell',lab);const toggle=()=>env.classList.toggle('open');shell.addEventListener('click',toggle);shell.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();toggle()}});qa('[data-mail-mode]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-mail-mode]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');mode=b.dataset.mailMode;render()}));q('[data-mail-run]',lab).addEventListener('click',()=>{stop?.();const nodes=qa('.request-road-node',lab),cap=q('.request-road-caption',lab);stop=animateNodes(nodes,cap,[['React',`Frontend prepares ${mode==='post'?'POST /briefings':'GET /briefings/42'}.`],['HTTPS','The web message travels over an encrypted connection.'],['API route','FastAPI matches the method + path to the right backend handler.'],['Python','Backend logic validates, checks permission, uses data/services, and decides the result.'],['Response','Backend sends status + data back; React updates the screen.']])})}

    const backend=q('#backend');if(backend&&!q('.backend-mission',backend)){const tasks=[{title:'Create a private briefing',ticket:'POST /briefings with title + visibility',steps:['validate','auth','logic','data','respond']},{title:'Run an OSINT search',ticket:'POST /search with a query',steps:['validate','auth','logic','data','respond']}];let ti=0,si=0;const lab=mount(backend,`<div class="study-deep-lab"><div class="study-deep-head"><div><span class="study-deep-tag">you are the backend</span><h3>React just handed you a request. Don’t panic. Do your jobs in order.</h3></div><p>The backend is mostly a boring adult: check the request, check permission, run logic, use storage/services if needed, send a clear answer.</p></div><div class="backend-mission"><div class="backend-inbox"><div class="study-deep-actions"><button class="active" data-backend-task="0" type="button">Spaces</button><button data-backend-task="1" type="button">OSINT</button></div><div class="ticket"><strong data-ticket-title></strong><span data-ticket-copy></span></div><p><b data-backend-count>1 / 5</b> · choose the next backend job.</p></div><div><div class="backend-choice-grid"><button class="backend-choice" data-backend-choice="validate"><strong>1. Validate input</strong><small>Is the request shaped correctly?</small></button><button class="backend-choice" data-backend-choice="auth"><strong>2. Check identity/permission</strong><small>Who is this and may they do it?</small></button><button class="backend-choice" data-backend-choice="logic"><strong>3. Run app logic</strong><small>Python/service/AI work.</small></button><button class="backend-choice" data-backend-choice="data"><strong>4. Use data/services</strong><small>Read/write Postgres or approved integrations.</small></button><button class="backend-choice" data-backend-choice="respond"><strong>5. Send response</strong><small>Status + allowed result back to frontend.</small></button><button class="backend-choice" data-backend-choice="html"><strong>🔥 Rewrite the entire frontend</strong><small>tempting only if you enjoy chaos</small></button></div><div class="backend-feedback"><b>Mission:</b> pick the first backend job.</div></div></div></div>`);const feedback=q('.backend-feedback',lab);const reset=()=>{si=0;const t=tasks[ti];q('[data-ticket-title]',lab).textContent=t.title;q('[data-ticket-copy]',lab).textContent=t.ticket;q('[data-backend-count]',lab).textContent='1 / 5';qa('[data-backend-choice]',lab).forEach(b=>b.classList.remove('correct','wrong'));feedback.innerHTML='<b>Mission:</b> pick the first backend job.'};qa('[data-backend-task]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-backend-task]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');ti=Number(b.dataset.backendTask);reset()}));qa('[data-backend-choice]',lab).forEach(b=>b.addEventListener('click',()=>{const expected=tasks[ti].steps[si],picked=b.dataset.backendChoice;b.classList.remove('correct','wrong');if(picked===expected){b.classList.add('correct');si++;q('[data-backend-count]',lab).textContent=`${Math.min(si+1,5)} / 5`;const good={validate:'Yep. Bad input should die early before it poisons deeper logic.',auth:'Yep. Private work needs identity + permission before sensitive data moves.',logic:'Yep. Now the actual Python/service/AI job can run.',data:'Yep. Storage and approved services help with durable/external information.',respond:'Done 🎯. Send the frontend a clear answer; do not make it guess what happened.'};feedback.innerHTML=`<b>Correct:</b> ${good[picked]}`;if(si===5)feedback.innerHTML='<b>Backend survived:</b> validate → permission → logic → data/services → response. Boring is beautiful.'}else{b.classList.add('wrong');const why={html:'The backend does not need to rebuild React for every request. It returns data/results; the frontend renders known UI.',validate:'Validation belongs first because malformed input should be rejected before deeper work.',auth:'Permission matters, but first make sure the request itself is valid enough to process.',logic:'App logic should not run before basic validation and required permission checks.',data:'Do not touch sensitive storage before you know the request/user is allowed to get there.',respond:'You cannot answer before doing the work. That would be customer support energy.'};feedback.innerHTML=`<b>Why that is wrong:</b> ${why[picked]||'Follow the backend job order.'}`}}));reset()}

    const database=q('#database');if(database&&!q('.db-query-grid',database)){const lab=mount(database,`<div class="study-deep-lab"><div class="study-deep-head"><div><span class="study-deep-tag">query animation</span><h3>Ask PostgreSQL a boringly specific question</h3></div><p>A query is just a request to the database. Here we ask for rows belonging to one user and watch matching records survive the filter.</p></div><div class="db-query-grid"><div class="db-query-console"><div class="study-deep-actions"><button class="active" data-query-user="1" type="button">user_id = 1</button><button data-query-user="2" type="button">user_id = 2</button></div><pre class="db-query-code">SELECT * FROM briefings\nWHERE user_id = 1;</pre><p>SQL = the language used to ask relational databases for records or changes.</p><button class="button primary" data-query-run type="button">▶ Run query</button></div><div class="db-result-table"><table><thead><tr><th>id</th><th>user_id</th><th>title</th></tr></thead><tbody><tr data-db-user="1"><td>42</td><td>1</td><td>Morning Brief</td></tr><tr data-db-user="2"><td>51</td><td>2</td><td>Family Plan</td></tr><tr data-db-user="1"><td>73</td><td>1</td><td>OSINT Case Brief</td></tr></tbody></table><div class="db-query-caption"><b>Waiting:</b> choose a user and run the query.</div></div></div></div>`);let user='1';qa('[data-query-user]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-query-user]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');user=b.dataset.queryUser;q('.db-query-code',lab).textContent=`SELECT * FROM briefings\nWHERE user_id = ${user};`}));q('[data-query-run]',lab).addEventListener('click',()=>{qa('tbody tr',lab).forEach(r=>{r.classList.remove('match','reject');setTimeout(()=>r.classList.add(r.dataset.dbUser===user?'match':'reject'),140)});const count=qa(`tbody tr[data-db-user="${user}"]`,lab).length;q('.db-query-caption',lab).innerHTML=`<b>Result:</b> PostgreSQL returned ${count} matching row${count===1?'':'s'}. Python/backend can now decide what data is safe/useful to send onward.`})}
  }

  if(path==='/study/python/'){
    const boss=q('#boss');if(boss&&!q('.code-conveyor',boss)){const order=['def build_case(query):','    result = search_sources(query)','    return result'];const lab=mount(boss,`<div class="study-deep-lab"><div class="study-deep-head"><div><span class="study-deep-tag">drag code · tap works too</span><h3>Build a tiny backend function, then execute it line by line</h3></div><p>Desktop: drag the blocks. Phone: tap them in order. You are learning the flow before syntax gets to act important.</p></div><div class="code-conveyor"><div class="code-bank"><strong>Code blocks</strong>${['    return result','def build_case(query):','    result = search_sources(query)'].map(x=>`<button class="draggable-code" draggable="true" data-code-block="${x.replaceAll('"','&quot;')}">${x}</button>`).join('')}</div><div class="code-stage"><strong>Your function</strong><div class="code-drop-zone" data-code-drop></div><div class="study-deep-actions"><button data-code-check type="button">Check order</button><button class="primary" data-code-run type="button">▶ Execute</button><button data-code-reset type="button">Reset</button></div><div class="code-exec-caption"><b>Tip:</b> define the function, do the work, return the result.</div></div></div></div>`);const bank=q('.code-bank',lab),drop=q('[data-code-drop]',lab),caption=q('.code-exec-caption',lab);let drag=null;const moveToDrop=b=>{if(b.parentElement!==drop)drop.appendChild(b)};qa('.draggable-code',lab).forEach(b=>{b.addEventListener('click',()=>moveToDrop(b));b.addEventListener('dragstart',()=>{drag=b;b.classList.add('dragging')});b.addEventListener('dragend',()=>b.classList.remove('dragging'))});drop.addEventListener('dragover',e=>e.preventDefault());drop.addEventListener('drop',e=>{e.preventDefault();if(drag)moveToDrop(drag)});q('[data-code-check]',lab).addEventListener('click',()=>{const got=qa('.draggable-code',drop).map(b=>b.dataset.codeBlock);const ok=got.length===order.length&&got.every((x,i)=>x===order[i]);caption.innerHTML=ok?'<b>Correct 🎯:</b> define → work → return. You just built a function without staring at a blank editor.':'<b>Not yet:</b> Python needs the function definition first. The search happens inside it. return sends the finished result back.'});q('[data-code-run]',lab).addEventListener('click',()=>{const lines=qa('.draggable-code',drop);if(!lines.length){caption.innerHTML='<b>Nothing to execute:</b> move some code into the function first 😭.';return}let i=0;lines.forEach(x=>x.classList.remove('active'));const explain=[['Define','Create a reusable function named build_case with an input slot called query.'],['Work','Call search_sources using the query and store the result.'],['Return','Send that result back to whatever called the function.']];const show=()=>{lines.forEach((x,n)=>x.style.boxShadow=n===i?'0 0 0 2px #55adff':'');caption.innerHTML=`<b>${explain[i]?.[0]||'Line'}:</b> ${explain[i]?.[1]||'Python executes this line.'}`};show();const t=setInterval(()=>{i++;if(i>=lines.length){clearInterval(t);lines.forEach(x=>x.style.boxShadow='');return}show()},850)});q('[data-code-reset]',lab).addEventListener('click',()=>{qa('.draggable-code',drop).forEach(b=>bank.appendChild(b));caption.innerHTML='<b>Reset:</b> tap or drag the blocks into the function.'})}
  }

  if(path==='/study/environment/'){
    const backend=q('#backend');if(backend&&!q('.network-map',backend)){const lab=mount(backend,`<div class="study-deep-lab"><div class="study-deep-head"><div><span class="study-deep-tag">networking without the CCNA jump-scare</span><h3>What actually happens when the browser talks to your backend</h3></div><p>You do not need the whole internet textbook yet. Learn the road: domain name → address → secure connection → web request → backend service → response.</p></div><div class="network-map"><article class="network-node"><span>01</span><strong>Domain</strong><small>db.cmxchat.com</small></article><article class="network-node"><span>02</span><strong>DNS</strong><small>name → IP address</small></article><article class="network-node"><span>03</span><strong>HTTPS/TLS</strong><small>encrypted connection</small></article><article class="network-node"><span>04</span><strong>HTTP request</strong><small>method + path + data</small></article><article class="network-node"><span>05</span><strong>Backend service</strong><small>FastAPI on its network door</small></article><article class="network-node"><span>06</span><strong>Response</strong><small>status + data return</small></article></div><div class="network-caption"><b>Dumb-simple analogy:</b> DNS finds the building, a port helps find the service door, HTTPS locks the conversation, HTTP is the message format.</div><div class="study-deep-actions"><button class="primary" data-network-run type="button">▶ Send browser → backend</button></div></div>`);let stop;q('[data-network-run]',lab).addEventListener('click',()=>{stop?.();stop=animateNodes(qa('.network-node',lab),q('.network-caption',lab),[['Domain','You start with a human-friendly name.'],['DNS','DNS looks up which network address currently serves that name.'],['HTTPS/TLS','Browser and server establish an encrypted connection so traffic is protected in transit.'],['HTTP','The browser sends a structured request such as POST /search.'],['Backend','Traffic reaches the service that can handle the API request. Ports help separate services on a machine/network interface.'],['Response','The server sends an HTTP status and allowed data back to the browser.']])})}

    const tools=q('#tools');if(tools&&!q('.ai-job-grid',tools)){const lab=mount(tools,`<div class="study-deep-lab"><div class="study-deep-head"><div><span class="study-deep-tag">AI is a worker, not your entire building</span><h3>Which jobs should AI do, and which jobs should boring code keep?</h3></div><p>AI is great at language, classification, summarizing and flexible reasoning. Your backend still owns permissions, validation, durable records and deterministic rules.</p></div><div class="ai-job-grid"><button class="ai-job" data-ai-job="ai"><strong>Summarize 12 retrieved OSINT sources</strong><small>AI can help turn evidence into readable synthesis.</small></button><button class="ai-job" data-ai-job="code"><strong>Decide whether user 1 may read user 2’s private case</strong><small>Permission rule belongs in backend authorization.</small></button><button class="ai-job" data-ai-job="ai"><strong>Suggest which known briefing cards best fit today</strong><small>AI can suggest structure/data for existing UI components.</small></button><button class="ai-job" data-ai-job="code"><strong>Store the authoritative user ID</strong><small>Durable identity belongs in controlled data/backend logic.</small></button></div><div class="backend-feedback" data-ai-feedback><b>Pick a job.</b> The point is to stop treating “AI” like one giant mystery box.</div><div class="ai-pipeline"><article class="ai-pipe-node"><strong>User action</strong><small>question/search</small></article><article class="ai-pipe-node"><strong>Backend</strong><small>validate + permission</small></article><article class="ai-pipe-node"><strong>Context/tools</strong><small>approved data/sources</small></article><article class="ai-pipe-node"><strong>Model call</strong><small>prompt + context</small></article><article class="ai-pipe-node"><strong>Validate result</strong><small>rules/citations/shape</small></article><article class="ai-pipe-node"><strong>UI + storage</strong><small>show/save allowed result</small></article></div><div class="study-deep-actions"><button class="primary" data-ai-run type="button">▶ Animate AI feature</button></div></div>`);const feed=q('[data-ai-feedback]',lab);qa('[data-ai-job]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-ai-job]',lab).forEach(x=>x.classList.remove('good','bad'));const wants=b.dataset.aiJob==='ai';b.classList.add('good');feed.innerHTML=wants?'<b>Good AI job:</b> flexible language/reasoning is useful here, but the backend should still control what context the model receives and what happens with its output.':'<b>Good boring-code job:</b> identity, authorization and authoritative records should not depend on a model “feeling like” the answer is probably yes.'}));let stop;q('[data-ai-run]',lab).addEventListener('click',()=>{stop?.();stop=animateNodes(qa('.ai-pipe-node',lab),feed,[['User action','Spaces or OSINT starts with a normal user action.'],['Backend first','Validate input and check identity/permission before sending private context anywhere.'],['Context/tools','Gather only the approved sources, records or tool results needed for this task.'],['Model call','Send instructions + selected context to the AI model. This is where token/latency/cost concerns can appear.'],['Validate output','Check structure, citations, safety and deterministic rules before trusting the output downstream.'],['Use result','Return it to React and optionally store an approved result. The AI does not become the database.']])})}

    const recovery=q('#recovery');if(recovery&&!q('.debug-shell',recovery)){const scenarios={api:{name:'Frontend says “Failed to fetch”',answer:'network',logs:['[browser] POST https://app.example/api/search','[browser] TypeError: Failed to fetch','[backend] no request received'],why:'The backend never saw a request. Investigate URL/DNS/network/CORS/service reachability before rewriting Python.'},server:{name:'500 Internal Server Error',answer:'backend',logs:['[browser] POST /api/search → 500','[backend] ERROR KeyError: source_url','[backend] request_id=8f2 failed'],why:'The request reached the backend and Python threw an exception. This is backend logic/data-shape territory.'},db:{name:'Database connection refused',answer:'database',logs:['[backend] opening database connection','[db] connection refused on database service','[backend] 503 dependency unavailable'],why:'FastAPI is alive, but its database dependency is not reachable. Check database service/config/network before blaming React.'},auth:{name:'401 Unauthorized',answer:'auth',logs:['[browser] GET /api/private-case → 401','[backend] authentication proof missing/invalid','[db] no private case query executed'],why:'The backend deliberately refused the request before touching private data. Investigate login/session/token flow.'}};let key='api';const lab=mount(recovery,`<div class="study-deep-lab"><div class="study-deep-head"><div><span class="study-deep-tag">debugging detective</span><h3>The bug is lying somewhere. Logs usually lie less.</h3></div><p>Pick a scenario, read the clues, then identify the broken layer. Wrong answers explain why they do not fit the evidence.</p></div><div class="debug-shell"><div class="debug-terminal"></div><div class="debug-panel"><h4 data-debug-title></h4><div class="debug-scenarios">${Object.entries(scenarios).map(([k,v])=>`<button type="button" data-debug-scenario="${k}" class="${k==='api'?'active':''}">${v.name.split(' ')[0]}</button>`).join('')}</div><p style="color:var(--muted);font-size:.72rem">Which layer should you investigate first?</p><div class="debug-layer-grid"><button data-debug-layer="frontend">Frontend</button><button data-debug-layer="network">Network/API path</button><button data-debug-layer="backend">Backend/Python</button><button data-debug-layer="database">Database</button><button data-debug-layer="auth">Auth</button></div><div class="debug-explanation"><b>Read the logs first.</b> Guessing is expensive and emotionally dramatic.</div></div></div></div>`);const term=q('.debug-terminal',lab),title=q('[data-debug-title]',lab),ex=q('.debug-explanation',lab);const render=()=>{const s=scenarios[key];title.textContent=s.name;term.innerHTML=s.logs.map((x,i)=>`<div class="${x.includes('ERROR')||x.includes('refused')||x.includes('401')||x.includes('500')?'bad':i===0?'info':'muted'}">${x}</div>`).join('');ex.innerHTML='<b>Read the clues:</b> where did the request get to before things went sideways?'};qa('[data-debug-scenario]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-debug-scenario]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');key=b.dataset.debugScenario;render()}));qa('[data-debug-layer]',lab).forEach(b=>b.addEventListener('click',()=>{const s=scenarios[key],ok=b.dataset.debugLayer===s.answer;ex.innerHTML=ok?`<b>Yep 🎯:</b> ${s.why}`:`<b>Nope, and here’s why:</b> ${b.dataset.debugLayer==='frontend'?'The browser can display the failure without being the layer that caused it. ':b.dataset.debugLayer==='backend'?'Check whether the request even reached Python before blaming Python. ':b.dataset.debugLayer==='database'?'Look for evidence that database access was attempted. ':b.dataset.debugLayer==='auth'?'A 401/403 points toward identity/permission; other errors need different clues. ':'Networking is the road. If the logs show the backend handled the request, the road probably worked. '}${s.why}` }));render()}

    const project=q('#project');if(project&&!q('.file-mission-grid',project)){const tasks=[{q:'You need a new GET /cases endpoint. Where do you start?',a:'routes',why:'Routes are the HTTP-facing doors. The route can then call reusable data/business functions.'},{q:'You need reusable code to read/update stored cases. Where does that job belong?',a:'crud',why:'CRUD/data helpers centralize common database operations so routes do not rewrite them.'},{q:'You changed backend API routes and need matching frontend request helpers.',a:'client',why:'The generated client is regenerated from OpenAPI. Do not manually freestyle generated files.'},{q:'A route needs the current logged-in user.',a:'deps',why:'Dependency helpers can provide reusable request context such as the current authenticated user.'},{q:'You need to describe stored/app data fields.',a:'models',why:'Models describe structured application/database data in this environment.'}];let i=0;const lab=mount(project,`<div class="study-deep-lab"><div class="study-deep-head"><div><span class="study-deep-tag">real environment file hunt</span><h3>Stop seeing folders. Start seeing jobs.</h3></div><p>You do not need to memorize paths like a hostage. Match a change to the file/folder responsible for that kind of work.</p></div><div class="file-mission-grid"><div class="file-tree"><button data-file="frontend">frontend/src/</button><button data-file="client">  client/   # generated API helpers</button><button data-file="models">backend/app/models.py</button><button data-file="crud">backend/app/crud.py</button><button data-file="routes">backend/app/api/routes/</button><button data-file="deps">backend/app/api/deps.py</button></div><div class="file-task"><h4 data-file-question></h4><p data-file-count></p><div class="file-task-choices">${[['routes','api/routes/'],['crud','crud.py'],['models','models.py'],['deps','api/deps.py'],['client','frontend/src/client/']].map(x=>`<button type="button" data-file-answer="${x[0]}">${x[1]}</button>`).join('')}</div><div class="file-task-feedback"><b>Pick the job, not the scary filename.</b></div></div></div></div>`);const draw=()=>{const t=tasks[i%tasks.length];q('[data-file-question]',lab).textContent=t.q;q('[data-file-count]',lab).textContent=`Mission ${i+1} / ${tasks.length}`;q('.file-task-feedback',lab).innerHTML='<b>Pick the job, not the scary filename.</b>'};qa('[data-file-answer]',lab).forEach(b=>b.addEventListener('click',()=>{const t=tasks[i%tasks.length],ok=b.dataset.fileAnswer===t.a;q('.file-task-feedback',lab).innerHTML=ok?`<b>Correct:</b> ${t.why}`:`<b>Why that is wrong:</b> ${b.dataset.fileAnswer==='client'?'Generated client code mirrors the backend API; it is not where backend logic lives.':b.dataset.fileAnswer==='routes'?'Routes handle HTTP-facing entry points, not every reusable data job.':b.dataset.fileAnswer==='crud'?'CRUD is about stored-record operations, not every application concern.':b.dataset.fileAnswer==='models'?'Models describe data; they do not receive HTTP requests.':'deps.py provides reusable dependencies/context; it is not the home for every route or database operation.'}`;if(ok){i++;setTimeout(draw,850)}}));draw()}
  }

  /* Explain wrong answers on existing quizzes without hijacking their original behavior. */
  const attachWhy=(selector,feedbackSelector,explain)=>qa(selector).forEach(btn=>btn.addEventListener('click',()=>{setTimeout(()=>{const msg=explain(btn);if(!msg)return;const fb=q(feedbackSelector);if(!fb)return;let why=q('.study-why-wrong',fb.parentElement);if(!why){why=document.createElement('div');why.className='study-why-wrong';fb.insertAdjacentElement('afterend',why)}why.innerHTML=`<b>Why:</b> ${msg}`},20)}));
  if(path==='/study/'){attachWhy('[data-predict-react]','#reactFeedback',b=>b.dataset.predictReact==='components'?'That works because the frontend owns reusable components and the backend/AI can return data + structure.':b.dataset.predictReact==='database'?'PostgreSQL stores data. It does not design the screen. React owns the interface.':'Rewriting the whole frontend every morning would be brittle, slow and impossible to keep consistent. Reuse known components instead.');attachWhy('[data-role-target]','#roleFeedback',b=>`Ask which bucket this job belongs to: FastAPI = web/API plumbing, Python = app decisions/logic, PostgreSQL = durable storage. You picked ${b.textContent.trim()}.`)}
  if(path==='/study/python/'){attachWhy('[data-decision-answer]','#decisionFeedback',b=>`Read the condition as a yes/no sentence first. Compare the current urgency number to 7, then choose the branch. Syntax can wait.`);q('#checkParsons')?.addEventListener('click',()=>{setTimeout(()=>{const fb=q('#parsonsFeedback');if(!fb||fb.classList.contains('good'))return;let why=q('.study-why-wrong',fb.parentElement);if(!why){why=document.createElement('div');why.className='study-why-wrong';fb.insertAdjacentElement('afterend',why)}why.innerHTML='<b>Why:</b> Python must know the function exists before it can run instructions inside it, and return belongs after the value is created.'},20)})}
})();