(()=>{
  if(window.__studyIndexTeachLoaded)return;window.__studyIndexTeachLoaded=true;
  const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];
  const extra={
    'Frontend':['Why it exists','People need a visible place to interact with the app. The frontend turns data and actions into screens, forms, cards and feedback.','Do not confuse it with','The backend. The frontend can ask for work, but private rules, secrets and database credentials belong elsewhere.','Say it out loud','“Frontend is the part in the browser that shows things and reacts to me.”'],
    'Backend':['Why it exists','The app needs a private place to enforce rules, process requests, check permissions, talk to databases and call protected services.','Do not confuse it with','The database. The backend decides what should happen. The database mainly stores and retrieves durable data.','Say it out loud','“Backend is private code that receives requests, does the real work and decides what can come back.”'],
    'React':['Why it exists','Large interfaces become easier to manage when the screen is built from reusable components that can update when data or state changes.','Do not confuse it with','A separate browser language. React ultimately uses JavaScript and produces normal browser UI.','Say it out loud','“React helps me build the frontend from reusable pieces that update when information changes.”'],
    'Component':['Why it exists','You should not copy-paste the same card or control everywhere. A component gives one reusable UI piece a clear job.','Do not confuse it with','A whole page. A page can be made from many components.','Say it out loud','“A component is one reusable screen piece, like WeatherCard or SearchPanel.”'],
    'TypeScript':['Why it exists','JavaScript is flexible enough to let bad assumptions survive until runtime. TypeScript lets tools catch many data-shape mistakes earlier.','Do not confuse it with','A replacement for JavaScript. TypeScript is compiled/transformed into JavaScript the browser can run.','Say it out loud','“TypeScript is JavaScript with extra information that lets tools warn me when data does not match what I expected.”'],
    'Props':['Why it exists','Reusable components need a way to receive different information without becoming different components.','Do not confuse it with','State. Props are handed in from outside the component; state is current information the UI is keeping track of.','Say it out loud','“Props are data I hand into a reusable component.”'],
    'State':['Why it exists','Interactive screens need to remember things such as what is selected, whether a panel is open or what result is currently loaded.','Do not confuse it with','Long-term database storage. Frontend state can disappear on refresh unless you deliberately persist it.','Say it out loud','“State is what the frontend is remembering right now.”'],
    'API':['Why it exists','Two pieces of software need a predictable contract for asking for data or actions without knowing each other’s entire internal code.','Do not confuse it with','An AI API or a paid service. An API is simply an interface for software communication.','Say it out loud','“An API is a controlled way for software to ask other software for something.”'],
    'Endpoint':['Why it exists','A backend can do many jobs, so each job needs a specific address and HTTP method.','Do not confuse it with','The whole API. One API contains many endpoints.','Say it out loud','“An endpoint is one exact API door, like GET /briefings/42.”'],
    'HTTP':['Why it exists','Browsers and servers need shared rules for formatting and exchanging web requests and responses.','Do not confuse it with','The logic inside your backend. HTTP carries the message; FastAPI/Python decide what to do with it.','Say it out loud','“HTTP is the rulebook for web messages going between client and server.”'],
    'JSON':['Why it exists','Software needs a simple text format for structured labeled data that can travel over HTTP.','Do not confuse it with','A database. JSON is a data format, not long-term storage.','Say it out loud','“JSON is a neat text format for sending labeled data like id, title and status.”'],
    'OpenAPI':['Why it exists','Humans can read endpoint docs, but tooling also needs a machine-readable description of methods, paths, inputs and outputs.','Do not confuse it with','The API itself. OpenAPI describes the API contract.','Say it out loud','“OpenAPI is a machine-readable menu describing my backend doors and data shapes.”'],
    'Generated API client':['Why it exists','If the backend already describes its API, the frontend can generate matching TypeScript helpers instead of hand-writing every request and type.','Do not confuse it with','App code you casually edit. Generated files are outputs; fix the backend contract and regenerate them.','Say it out loud','“The generated client is auto-made frontend helper code that matches the backend API.”'],
    'DNS':['Why it exists','Humans remember names better than numeric network addresses, so DNS maps a domain name to where a service can be reached.','Do not confuse it with','Hosting. DNS points traffic; it does not itself run your application.','Say it out loud','“DNS is the name-to-address system that helps a domain find the service.”'],
    'Port':['Why it exists','One machine can run several network services. Ports help traffic reach the right service on that machine.','Do not confuse it with','A physical cable port. This is a numbered network door.','Say it out loud','“A port is a numbered door for one service, like 5173 for the frontend or 8000 for FastAPI in development.”'],
    'HTTPS':['Why it exists','Sensitive web traffic should be encrypted while it travels between browser and server.','Do not confuse it with','Authentication. HTTPS protects the trip; login proves identity.','Say it out loud','“HTTPS is HTTP traveling through an encrypted connection.”'],
    'TLS':['Why it exists','TLS provides the cryptographic protection used by HTTPS so other people on the network cannot casually read or alter the traffic.','Do not confuse it with','A login token. TLS protects the connection, not the user account state.','Say it out loud','“TLS is the encryption layer that gives HTTPS its lock.”'],
    'CORS':['Why it exists','Browsers enforce origin rules so a random website cannot freely call another site’s protected API with browser credentials.','Do not confuse it with','A firewall for every kind of traffic. CORS is mainly a browser security rule around cross-origin requests.','Say it out loud','“CORS is the browser bouncer deciding which origins are allowed to call an API.”'],
    'Python':['Why it exists','It is the backend programming language in this learning environment. You use it to express logic, validation, data processing and service behavior.','Do not confuse it with','FastAPI. Python is the language; FastAPI is a framework written for Python web applications.','Say it out loud','“Python is the language I use to write backend instructions.”'],
    'FastAPI':['Why it exists','Writing raw HTTP server plumbing yourself would waste time and create room for mistakes. FastAPI maps web routes to Python functions and helps with validation and documentation.','Do not confuse it with','The Python logic itself. FastAPI routes and validates requests; your Python code still decides the application behavior.','Say it out loud','“FastAPI connects HTTP doors to Python functions.”'],
    'CRUD':['Why it exists','Most stored records need the same four basic operations: create them, read them, update them and delete them. Naming that pattern makes data code easier to organize.','Do not confuse it with','All business logic. CRUD is the boring data work, not the entire app.','Say it out loud','“CRUD means create, read, update and delete. Four boring database jobs.”'],
    'Model':['Why it exists','The app needs a consistent description of what a kind of data looks like, such as fields and types for a User or Briefing.','Do not confuse it with','A database row itself. A model is the code description; rows are stored records.','Say it out loud','“A model is a blueprint describing the shape of one kind of data.”'],
    'Pydantic':['Why it exists','Incoming and outgoing data should match expected types and required fields before deeper code trusts it.','Do not confuse it with','PostgreSQL. Pydantic validates Python data shapes; PostgreSQL stores records.','Say it out loud','“Pydantic checks whether Python data matches the shape I said it should have.”'],
    'SQLModel':['Why it exists','This stack wants Python models that can participate in validation and database work without maintaining completely separate definitions for everything.','Do not confuse it with','PostgreSQL itself. SQLModel is a Python layer that helps code work with relational database records.','Say it out loud','“SQLModel lets Python model classes describe data that also maps to database tables.”'],
    'PostgreSQL':['Why it exists','The application needs durable structured storage that survives browser refreshes and server restarts. PostgreSQL handles tables, relationships, transactions and queries.','Do not confuse it with','Aiven. PostgreSQL is the database software; Aiven is the managed service hosting it in this environment.','Say it out loud','“PostgreSQL is the app’s organized long-term memory.”'],
    'SQL':['Why it exists','Relational databases need a language for asking for rows, inserting records, updating them and changing structure.','Do not confuse it with','Python. Python can cause database work to happen, while SQL is the language the relational database understands.','Say it out loud','“SQL is how you ask a relational database questions or tell it to change records.”'],
    'ORM':['Why it exists','Developers often want to work with typed programming-language objects while still storing data in relational tables. An ORM translates many of those operations into SQL.','Do not confuse it with','The database driver. The ORM works at a higher level; the driver handles the actual connection/protocol details.','Say it out loud','“An ORM lets my Python objects map to database rows and queries.”'],
    'Migration':['Why it exists','Database structure changes over time. Migrations record those changes in order so every environment can move from the old schema to the new one deliberately.','Do not confuse it with','Moving data to a new hosting company. Here migration usually means a versioned schema change.','Say it out loud','“A migration is a recorded step that changes the database structure.”'],
    'Authentication':['Why it exists','Private applications need to know which user is making a request before trusting identity-specific actions.','Do not confuse it with','Authorization. Authentication proves identity; authorization checks permission.','Say it out loud','“Authentication asks: who are you?”'],
    'Authorization':['Why it exists','Knowing who someone is does not mean they should be allowed to access every resource or action.','Do not confuse it with','Logging in. Login can authenticate you while a later permission check still blocks access.','Say it out loud','“Authorization asks: okay, I know who you are, but are you allowed to do this?”'],
    'Session':['Why it exists','Users should not have to submit their password on every protected request. The application needs a continuing logged-in state across requests.','Do not confuse it with','A permanent account ID. Sessions can expire and are tied to a login state.','Say it out loud','“A session is the continuing state that lets the app recognize this browser as already logged in.”'],
    'Token':['Why it exists','A client sometimes needs a piece of proof it can send with later requests so the backend can verify prior authentication.','Do not confuse it with','AI tokens. Same word, completely different concept.','Say it out loud','“An auth token is one possible piece of proof carried with later requests.”'],
    'Docker':['Why it exists','Applications behave more predictably when the runtime and dependencies they expect are packaged in a repeatable environment.','Do not confuse it with','A virtual machine or your database host. Docker packages/runs containers; this project’s PostgreSQL is hosted on Aiven.','Say it out loud','“Docker gives my app a repeatable box with the runtime and dependencies it expects.”'],
    'Docker Compose':['Why it exists','Several containers/services often need coordinated configuration, networks, ports and environment values. Compose describes that group.','Do not confuse it with','A requirement that every service must be local. Your current handbook keeps PostgreSQL on Aiven.','Say it out loud','“Compose is a plan for running multiple Docker services together.”'],
    'Environment variable':['Why it exists','Configuration changes between local, staging and production, and secrets should not be hardcoded into source files.','Do not confuse it with','A normal Python variable inside code. Environment variables come from the runtime environment around the program.','Say it out loud','“An environment variable is configuration the app receives from outside its source code.”'],
    'Git':['Why it exists','Code changes need history, comparison and safe recovery points so you can understand what changed and undo mistakes.','Do not confuse it with','GitHub. Git is the version-control system; GitHub hosts repositories and collaboration around Git.','Say it out loud','“Git records my code changes and gives me recoverable checkpoints.”'],
    'Render':['Why it exists','The application needs a place on the internet where reviewed code can be built and run as a service.','Do not confuse it with','Aiven. Render runs the application service in this handbook; Aiven hosts PostgreSQL.','Say it out loud','“Render is where this app can be deployed to run publicly.”'],
    'AI model / LLM':['Why it exists','Some tasks involve flexible language understanding, summarization, classification or reasoning that would be painful to encode as fixed rules.','Do not confuse it with','Your backend, database or source of truth. The model should receive approved context and operate inside application rules.','Say it out loud','“The AI model is a flexible reasoning/generation tool the backend can call when that kind of task actually helps.”'],
    'Prompt':['Why it exists','The model needs instructions, context, constraints and an output goal for the current task.','Do not confuse it with','Permanent application logic. Critical permissions and deterministic rules should stay in code.','Say it out loud','“A prompt is the task and context I hand the model for this run.”'],
    'Context':['Why it exists','The model can only reason over information available in its current input or connected tools, so the workflow must deliberately supply relevant approved information.','Do not confuse it with','Long-term database memory. Context is what the model can see for this task.','Say it out loud','“Context is the information the AI gets to look at right now.”'],
    'Tool call':['Why it exists','The model itself cannot safely or reliably perform every real-world action. A tool call lets the application invoke a bounded function with structured inputs.','Do not confuse it with','Giving the model unrestricted shell or database access. Tools should be scoped and permissioned.','Say it out loud','“A tool call is the AI asking approved code to perform one specific action.”'],
    'RAG':['Why it exists','Models can be wrong or lack the latest/private information. Retrieval first can supply relevant documents or records before generation.','Do not confuse it with','Training the model. RAG retrieves information at request time and includes it as context.','Say it out loud','“RAG means look up relevant information first, then let the model answer using what was found.”'],
    'Spec Kit':['Why it exists','AI coding becomes safer and easier to review when intent, constraints, plan and tasks are written before implementation.','Do not confuse it with','A code generator you blindly run. It structures the development workflow.','Say it out loud','“Spec Kit makes us describe the feature before an agent starts improvising code.”'],
    'Serena':['Why it exists','As a repo grows, AI can miss symbol relationships when it only searches raw text. Serena gives code-aware navigation across functions, classes and references.','Do not confuse it with','A second coding model. It is a code-navigation/tooling layer the AI can use.','Say it out loud','“Serena helps the coding AI understand where functions and classes live and where they are used.”'],
    'MCP':['Why it exists','AI applications need a structured way to connect models with approved tools and data sources.','Do not confuse it with','Automatic permission to everything. A protocol connection still needs scoped tools and access rules.','Say it out loud','“MCP is a standard way to expose approved tools or data to an AI system.”']
  };

  function enrichTerms(){
    qa('#termGrid .index-term').forEach(card=>{
      if(q('.index-term-long',card))return;const name=q('h3',card)?.textContent?.trim();const d=extra[name];if(!d)return;
      const host=q('.index-term-extra > div',card)||q('.index-term-extra',card);if(!host)return;
      const block=document.createElement('div');block.className='index-term-long';block.innerHTML=`<div><span>${d[0]}</span><p>${d[1]}</p></div><div><span>${d[2]}</span><p>${d[3]}</p></div><div><span>${d[4]}</span><p><b>${d[5]}</b></p></div>`;host.appendChild(block);const note=document.createElement('span');note.className='depth-note';note.textContent='Longer explanation inside';q('.flip-hint',card)?.insertAdjacentElement('beforebegin',note)
    })
  }
  enrichTerms();new MutationObserver(enrichTerms).observe(q('#termGrid')||document.body,{childList:true,subtree:true});

  const projects=q('#projects');if(projects&&!q('.project-playground',projects)){
    const intro=document.createElement('div');intro.className='index-teach-intro';intro.innerHTML='<b>The rule:</b> do not memorize the arrows. Ask what job each arrow is doing. A person clicks something, the frontend decides whether it needs outside/private data, the request crosses the API boundary, backend code enforces rules and does work, storage/tools help when needed, then a response returns so the screen can update.';q('.index-heading',projects)?.insertAdjacentElement('afterend',intro);
    const scenarios={
      spaces:{title:'Load Personal Space',tag:'Spaces · reading private data',copy:'You open Personal Space. The browser needs your private saved briefing data, so the frontend cannot just invent it or query PostgreSQL directly.',why:'This example teaches frontend state, authentication, authorization, API requests, database reads and rendering in one flow.',steps:[['React route/component','The Personal screen is visible and decides it needs server data.','Frontend job: render UI and trigger data loading.'],['Generated client + TanStack Query','Frontend calls the generated API helper; Query manages loading/cache behavior.','This is the real stack version of a raw fetch().'],['HTTP request','A GET request travels to the backend with login proof.','Networking job: carry method, path, headers and any allowed data.'],['FastAPI route + deps','FastAPI matches the route; shared dependencies can resolve the current logged-in user.','Backend boundary: identify request + user before private data leaves.'],['Python / CRUD / SQLModel','Backend asks for records belonging to that user.','Application job: enforce ownership and perform data work.'],['Aiven PostgreSQL','PostgreSQL returns the saved rows.','Storage job: durable structured memory.'],['JSON response → React','Allowed data comes back and React renders Personal Space cards.','The screen updates because data changed, not because backend drew a new webpage.']]},
      setting:{title:'Change a briefing setting',tag:'Spaces · writing data',copy:'You toggle “priority alerts.” The frontend already knows the button changed, but the preference should survive refreshes and future sessions, so it must be saved.',why:'This teaches local UI state versus durable database state, PATCH/POST-style requests, validation and optimistic-looking interfaces.',steps:[['Toggle click','React can immediately know the checkbox is now on.','That is frontend state. It exists right now in the screen.'],['Request helper','Frontend sends the new preference to a backend endpoint.','Because durable preferences belong behind the API.'],['Validation + auth','FastAPI/Python checks the value shape and who owns this settings record.','Never trust browser input just because your own UI generated it.'],['Database update','CRUD/SQLModel updates the saved preference in PostgreSQL.','Now the setting survives page refreshes.'],['Response','Backend confirms the saved result or reports an error.','Frontend can keep or revert the visible toggle based on what actually happened.']]},
      osint:{title:'Run an OSINT search',tag:'OSINT · tools + evidence',copy:'You submit a person/company/domain query. The private backend can use approved research tools and optionally AI, then store a case or report without exposing provider credentials to the browser.',why:'This separates deterministic application rules from tool calls, AI reasoning, sources and durable case records.',steps:[['Search form','React collects the query and basic options.','Visible UI only knows what the user typed.'],['POST /search','Structured request crosses into the backend.','POST can carry query/options in a JSON body.'],['Permission + validation','Backend checks the user and whether the request is allowed/shaped correctly.','Do this before expensive or sensitive tool work.'],['Research tools','Python calls approved sources/services and collects raw evidence.','Tools fetch real information; the LLM is not magically browsing by thinking harder.'],['AI step if useful','Model can classify/summarize approved evidence with source context.','AI is one worker in the pipeline, not the permission system.'],['Store case/report','Relevant structured result and references can be written to PostgreSQL.','Durable memory means you can reopen the case later.'],['Return structured result','Frontend receives status/data and renders the research view.','The backend should return data, not secretly rewrite your whole React app.']]},
      ai:{title:'Build the morning AI briefing',tag:'Spaces · scheduled AI pipeline',copy:'Before you wake up, a backend job can gather allowed information, call tools/models, assemble a briefing data structure and save the result. Later the frontend simply requests that finished briefing.',why:'This is the clean answer to “is the backend building a whole new frontend every morning?” No. The frontend owns reusable components; backend/AI chooses data and structure.',steps:[['Scheduled backend job','A scheduler/background process starts without you clicking a button.','Not every backend action begins with a live browser request.'],['Gather approved context','Python loads allowed calendars, saved settings, weather/tools or other permitted inputs.','The permission boundary still matters when nobody is watching the screen.'],['AI + deterministic logic','AI can summarize/prioritize; code can enforce exact rules and output schema.','Use AI for flexible reasoning, code for hard guarantees.'],['Validate structure','Backend checks that the generated briefing matches supported module types and safe fields.','Never blindly trust generated output just because it came from your own model call.'],['Save briefing','PostgreSQL stores the result/metadata that should persist.','Now it is ready before the user opens Spaces.'],['User opens app','React requests today’s briefing through the API.','The morning page is assembled from known WeatherCard, AlertCard, ChartCard, etc.']]},
      login:{title:'Log in and open protected data',tag:'Auth · identity + permission',copy:'You type email and password once. The backend verifies the account, then later requests carry proof so you do not resend your password on every click.',why:'This teaches password hashes, authentication, tokens/sessions and the fact that login still does not grant access to every private record.',steps:[['Login form','Frontend collects email/password and sends them over HTTPS.','The browser should never store a readable server password database.'],['Backend lookup','Python finds the account and stored password hash.','The database stores a hash, not the readable password.'],['Password verification','Security code checks whether the supplied password matches the stored hash.','Authentication answers “who are you?”'],['Issue login proof','The app creates/returns session or token-style proof.','Exact mechanisms can differ; the purpose is continuing authenticated requests.'],['Protected request later','Browser sends proof when requesting private data.','You do not type the password again for every API call.'],['Authorization check','Backend verifies this user may access this exact resource.','Authenticated Jay can still be forbidden from someone else’s private Space.']]},
      error:{title:'The page says “500”',tag:'Debugging · find the broken layer',copy:'You clicked Load Briefing and the frontend shows an error. Seeing the error in the browser does not prove the frontend caused it.',why:'This teaches evidence-first debugging across layers instead of randomly editing whichever file you happen to be staring at.',steps:[['Browser request','Developer tools show GET /briefings/42 returned 500.','Now you know the request reached some backend response path.'],['Backend logs','Find the first meaningful exception in FastAPI/Python logs.','A 500 usually means server-side code failed unexpectedly.'],['Trace the failing operation','Maybe SQLModel could not query, a field assumption was wrong or a provider failed.','Follow evidence to one layer.'],['Fix one cause','Change the smallest relevant code/configuration.','Do not “fix” React, Docker and PostgreSQL all at once because anxiety said so.'],['Run tests + reproduce','Confirm the original request now returns the expected status/data.','A green feeling is not a test result.'],['Review diff','Check exactly what changed before shipping.','Recovery and understanding are part of development, not optional cleanup.']]}
    };
    const lab=document.createElement('div');lab.className='project-playground';lab.innerHTML=`<div class="project-scenario-tabs">${Object.entries(scenarios).map(([k,v],i)=>`<button type="button" data-project-scenario="${k}" class="${i===0?'active':''}">${v.title}</button>`).join('')}</div><div class="project-scenario-stage"><div class="project-scenario-copy"><span class="tiny" data-project-tag></span><h3 data-project-title></h3><p data-project-copy></p><div class="scenario-why" data-project-why></div></div><div class="project-scenario-flow" data-project-flow></div></div><div class="project-legend"><div><span>Frontend</span><p>Screen behavior, interaction and rendering.</p></div><div><span>Backend</span><p>Private rules, permissions and processing.</p></div><div><span>Data / tools</span><p>Durable records or approved external capabilities.</p></div><div><span>Network</span><p>The messages carrying requests and responses between layers.</p></div></div>`;projects.appendChild(lab);
    const draw=k=>{const s=scenarios[k];q('[data-project-tag]',lab).textContent=s.tag;q('[data-project-title]',lab).textContent=s.title;q('[data-project-copy]',lab).textContent=s.copy;q('[data-project-why]',lab).innerHTML=`<b>Why this matters:</b> ${s.why}`;q('[data-project-flow]',lab).innerHTML=s.steps.map((x,i)=>`<div class="scenario-step"><i>${String(i+1).padStart(2,'0')}</i><div><strong>${x[0]}</strong><small>${x[1]}</small><em>${x[2]}</em></div></div>`).join('');qa('[data-project-scenario]',lab).forEach(b=>b.classList.toggle('active',b.dataset.projectScenario===k))};qa('[data-project-scenario]',lab).forEach(b=>b.addEventListener('click',()=>draw(b.dataset.projectScenario)));draw('spaces');
  }
})();