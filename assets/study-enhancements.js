(()=>{
  if(window.__studyEnhancementsLoaded)return;window.__studyEnhancementsLoaded=true;
  const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];
  const path=(location.pathname.replace(/\/+$/,'')||'/')+'/';
  const safeGet=k=>{try{return localStorage.getItem(k)}catch{return null}},safeSet=(k,v)=>{try{localStorage.setItem(k,v)}catch{}};
  if(!q('link[href^="/assets/study-enhancements.css"]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/assets/study-enhancements.css?v=20260812-2';document.head.appendChild(l)}

  const definitions={
    'frontend':['The part of the app that runs in your browser and controls what you can see and interact with. In this environment, React and TypeScript organize that interface.','You open Personal Space, tap a card, and the screen changes. That visible experience is frontend work.'],
    'backend':['Private server-side code that receives requests, enforces rules, processes data, checks permissions, and talks to storage or other services.','You tap “Create briefing.” The backend decides whether you may do it, creates the briefing data, saves what needs saving, and answers the frontend.'],
    'react':['A JavaScript library for building interactive frontend interfaces from reusable pieces called components. React keeps what you see synchronized with changing data and state.','One WeatherCard can be reused in Personal, Family, and Business Spaces while receiving different data each time.'],
    'component':['A reusable piece of interface with a clear job. A component can be small like a button or larger like an entire briefing card.','WeatherCard, PriorityAlert, CalendarCard, and ChartCard can all be components that the frontend already knows how to render.'],
    'typescript':['JavaScript with extra type information. Those extra descriptions help tools catch mismatched data before the app runs.','If a briefing expects an id to be a number, TypeScript can complain when frontend code tries to treat it like a completely different shape.'],
    'props':['Information passed into a React component from the outside. Props let the same reusable component display different content.','WeatherCard gets New York data in one Space and London data in another. The card can stay the same while its props change.'],
    'state':['Information the frontend is currently keeping track of while the interface runs. When state changes, the visible interface can update.','Selected Space = Personal, panelOpen = true, or priority = High are all examples of state-like information.'],
    'tanstack router':['A frontend routing library. It helps decide which React screen should appear for a location or URL without asking the database to design the page.','Going from a dashboard screen to a settings screen can be handled by frontend routing.'],
    'tanstack query':['A frontend library for working with server data. It helps fetch, cache, refresh, and keep backend data synchronized with the interface.','The frontend loads your briefings, remembers the result for a while, and refreshes when that server data changes.'],
    'tailwind':['A styling system built around small utility classes. It helps developers compose spacing, sizing, layout, responsive behavior, and visual presentation.','A card can receive utilities for padding, rounded corners, grid layout, and mobile spacing without inventing a new CSS class for every tiny rule.'],
    'shadcn/ui':['A collection of reusable interface component patterns that developers can bring into a React project and customize.','Dialogs, buttons, dropdowns, forms, and other common interface pieces can start from reusable patterns instead of being rebuilt from zero.'],
    'api':['A defined way for software to communicate with software. In your current mental model, the backend exposes API doors and the frontend calls them.','The frontend asks the backend for your briefing through an API instead of directly opening PostgreSQL itself.'],
    'endpoint':['One specific API door, usually described by an HTTP method plus a path. Different doors handle different kinds of requests.','GET /briefings might ask for briefings, while POST /briefings might ask the backend to create one.'],
    'http request':['A structured message sent to a web server. It can include a method, path, headers, and sometimes a body containing data.','React sends POST /briefings with the information needed to create a briefing.'],
    'http response':['The structured answer a backend sends after handling a request. It can include a status code and returned data.','The backend answers “201 Created” with the new briefing id and data, then React updates the screen.'],
    'request body':['Data carried inside a request, especially with actions such as POST or PATCH. JSON is a common body format.','A create-briefing request body might contain a title, visibility, and selected settings.'],
    'json':['A common text format for structured data made from objects, lists, names, numbers, true/false values, and more.','The backend can answer with {"id":42,"title":"Morning Brief"} and the frontend can use those fields.'],
    'validation':['Checking whether incoming information follows the expected rules and shape before deeper work happens.','If an endpoint requires a title and receives no title, validation can reject the request early.'],
    'python':['The programming language used for backend logic in this learning environment. Python expresses the instructions that decide what the backend should do.','Python can gather briefing data, check permissions, transform information, and decide what should be saved.'],
    'fastapi':['A Python web framework that makes it easier to expose Python functions through HTTP API routes and handle common web plumbing.','FastAPI can connect POST /briefings to a Python function that creates a briefing.'],
    'framework':['Reusable code, structure, and conventions that handle common plumbing so you can focus on the application-specific behavior.','FastAPI handles much of the web-request machinery so you do not build a raw HTTP server from scratch for every endpoint.'],
    'route':['A backend mapping between a request location and code that handles it. In FastAPI, route code usually connects an HTTP method/path to a Python function.','GET /users/me can route to code that returns the currently authenticated user.'],
    'postgresql':['A relational database that stores durable structured data in tables and supports relationships and queries.','A users table can hold user records while a briefings table stores briefings linked back to the owning user.'],
    'table':['A database structure made of columns and rows. Columns describe fields; rows are individual records.','The briefings table might have id, user_id, title, and visibility columns.'],
    'row':['One individual record inside a database table.','Briefing id 42 with user_id 1 and title Morning Brief is one row.'],
    'column':['One named field shared by records in a table.','user_id is a column that can store which user owns each briefing row.'],
    'id':['A value used to uniquely identify a record. IDs also help other records point to it.','users.id = 1 identifies Jay. briefings.user_id = 1 can link a briefing back to Jay.'],
    'primary key':['The column or set of columns that uniquely identifies each row in a table.','users.id can be the primary key so every user record has a unique identity.'],
    'foreign key':['A field that points to a key in another table, creating a relationship between records.','briefings.user_id can point to users.id.'],
    'sql':['A language used to ask relational databases to read, insert, update, delete, and organize data.','A query can ask PostgreSQL for all briefing rows where user_id = 1.'],
    'orm':['A code layer that maps program objects/models to relational database structures so application code can work with records without writing raw SQL for every operation.','Python code can work with a Briefing model while the ORM translates operations into database queries.'],
    'migration':['A recorded change to database structure that can be reviewed and applied in order.','Adding a visibility column to briefings should be an explicit migration instead of secretly changing production data shape.'],
    'model':['A code-side description of a structured thing. Depending on the project, models can describe database records or API/data shapes.','A Briefing model may describe fields such as id, title, user_id, and visibility.'],
    'schema':['A description of expected structure. In APIs it can describe request/response data; in databases it can mean the structure of tables and constraints.','A create-briefing request schema can require a title and define which visibility values are valid.'],
    'crud':['Create, Read, Update, Delete: the four common categories of operations on stored records.','Create a briefing, read it, change its title, or delete it.'],
    'authentication':['Proving who a user is. Authentication happens before the backend can safely treat a request as belonging to a known account.','You log in, the backend verifies the password safely, and the browser receives continuing proof of that login.'],
    'authorization':['Checking what an authenticated user is allowed to see or do. Identity alone does not grant permission to every resource.','Jay can be logged in and still be denied access to another user’s private briefing.'],
    'password hash':['A one-way representation used to verify a password without storing the readable password itself.','The backend compares the supplied password safely against the stored hash instead of reading a plain-text password from the database.'],
    'token':['One possible piece of continuing login proof that a client can send with later requests. The exact token design depends on the authentication system.','After login, a protected request can carry proof so you do not send your password again every time.'],
    'session':['A continuing logged-in state. A server-side session often has an identifier that lets the server recognize which browser is making later requests.','Your browser sends its session proof and the backend recognizes that this is still Jay’s logged-in session.'],
    'openapi':['A machine-readable description of an HTTP API: its routes, methods, inputs, outputs, and data shapes. FastAPI can produce this description.','The project can use FastAPI’s OpenAPI description to generate matching TypeScript client functions.'],
    'generated api client':['Frontend code generated from the backend API description. It gives the frontend matching request functions and types without hand-maintaining every API call.','FastAPI describes GET /items, the client generator creates a readItems function, and React calls that generated function.'],
    'docker':['A tool for packaging an application and its runtime environment into repeatable containers. It helps the same setup run consistently in different places.','A FastAPI service can run in a container with the same dependencies locally and on a server.'],
    'docker + compose':['Docker packages services; Compose describes how multiple services run together.','FastAPI, PostgreSQL, and another required service can be started together as a repeatable local stack.'],
    'localhost':['A name that means “this computer.” Local development services often run on different numbered ports on localhost.','localhost:5173 can serve the frontend while localhost:8000 serves FastAPI.'],
    'port':['A numbered network door used by a running service. Different local services can listen on different ports.','5173 and 8000 let the frontend and backend run separately on the same computer.'],
    'environment variable':['A configuration value supplied outside the application source code. It is commonly used for URLs, environment-specific settings, and secrets.','DATABASE_URL can tell the backend which database to connect to without hardcoding production credentials into Python files.'],
    'dependency':['A library or package the project relies on. Dependencies need versions and a repeatable installation process.','FastAPI is a backend dependency; React and TanStack libraries are frontend dependencies.'],
    'git':['A version-control tool that records exact changes to files and lets you create recoverable checkpoints.','Before a large generated change, a clean Git state gives you a known point to compare or restore.'],
    'github':['A hosted collaboration/source-control platform built around Git repositories, branches, pull requests, reviews, and automation.','The repository is the source of truth for code history and reviewable changes.'],
    'staging':['A production-like environment used to test a release before real production users or data depend on it.','Deploy a new backend revision to staging, test it there, then promote the known revision to production.'],
    'production':['The live environment serving real users or real workloads. Production changes deserve stricter review and recovery planning.','A database migration in production should have a backup and rollback/recovery plan before it runs.'],
    'render':['A hosting platform. The handbook includes a guide for deploying this environment to Render. The exact service settings and commands should come from that guide.','Conceptually: a reviewed Git revision is built, configured with environment values, started as a service, then observed through health and logs.'],
    'spec kit':['A spec-driven development toolkit. The useful mental model is: make intent and constraints explicit before an AI agent starts changing code.','Describe a feature, create a technical plan, break it into tasks, then implement and validate against the written intent.'],
    'serena':['A semantic code-agent toolkit that helps coding agents navigate and edit code using symbols and code structure instead of treating the repository as one giant text blob.','An agent can find a specific function or class and make a bounded change instead of blindly searching and rewriting unrelated files.'],
    'mcp':['A protocol for connecting AI systems to external tools and data in a structured way. Tool access still needs permissions and boundaries.','A coding assistant can receive approved repository or tool capabilities through an MCP integration without being given unrestricted machine access.'],
    'pytest':['A widely used Python testing framework and a strong default candidate in the environment specification.','A test can call backend logic and verify that creating a briefing produces the expected result.'],
    'alembic':['A database migration tool commonly used with SQLAlchemy-style Python database models. It is listed as a strong candidate in the environment specification.','A migration file can record the addition of a visibility column and apply that change in a controlled order.'],
    'uvicorn':['An ASGI server commonly used to run FastAPI applications. It is listed as a likely server choice in the environment specification.','During development, Uvicorn can run the FastAPI application so requests can reach your Python code.'],
    'cors':['Browser security rules controlling which website origins may make certain requests to a server.','A frontend at one origin may need the backend to explicitly allow that origin during development or production.'],
    'rate limiting':['Rules that restrict how frequently a client can perform an action, helping protect services from abuse or runaway traffic.','A login endpoint might limit repeated attempts from the same source.'],
    'https':['Encrypted HTTP. HTTPS protects information while it travels between the browser and server.','Login credentials should travel over HTTPS instead of readable network traffic.'],
    'websockets':['A long-lived connection that allows client and server to send messages to each other without creating a new normal HTTP request for every update.','A realtime chat interface can receive new messages over an open WebSocket connection.'],
    'redis':['A fast in-memory data store often used for caching, short-lived data, queues, or coordination when a project actually needs it.','A backend may cache an expensive result briefly so repeated requests do not recompute it every time.'],
    'ci/cd':['Automation that checks, builds, and sometimes releases software when code changes. CI focuses on integration/validation; CD focuses on delivery/deployment.','A pull request can automatically run tests before a release is allowed.'],
    'reverse proxy':['A server that sits in front of application services and forwards traffic to the right backend while often handling TLS, routing, or other edge concerns.','Requests to an app domain can reach a reverse proxy first, which forwards API traffic to FastAPI.'],
    'load balancing':['Distributing incoming traffic across multiple running instances so one machine does not have to handle everything.','Several backend instances can share requests when an application grows.']
  };
  const lookup=name=>definitions[name.toLowerCase().trim()]||null;

  let toastTimer;const toast=(title,text='')=>{let t=q('.study-achievement-toast');if(!t){t=document.createElement('div');t.className='study-achievement-toast';document.body.appendChild(t)}t.innerHTML=`<b>${title}</b>${text?` · ${text}`:''}`;requestAnimationFrame(()=>t.classList.add('show'));clearTimeout(toastTimer);toastTimer=setTimeout(()=>t.classList.remove('show'),2400)};

  const definitionState=new Set(JSON.parse(safeGet('study-definition-opened')||'[]'));
  const updateDefinitionMission=()=>{const el=q('[data-definition-mission]');if(el)el.textContent=`${Math.min(3,definitionState.size)} / 3 terms explored`;if(definitionState.size>=3&&!safeGet('study-definition-badge')){safeSet('study-definition-badge','1');toast('Translator unlocked','you opened 3 definitions')}};
  qa('.term').forEach(term=>{if(term.classList.contains('study-definition-ready'))return;const name=q('strong',term)?.textContent?.trim()||'';const d=lookup(name);if(!d)return;term.classList.add('study-definition-ready');term.tabIndex=0;term.setAttribute('role','button');term.setAttribute('aria-expanded','false');const hint=document.createElement('span');hint.className='study-term-hint';hint.textContent='Tap for a fuller explanation';const extra=document.createElement('div');extra.className='study-term-extra';extra.innerHTML=`<div><p>${d[0]}</p><div class="study-term-example"><strong>Example</strong>${d[1]}</div></div>`;term.append(hint,extra);const toggle=()=>{const open=term.getAttribute('aria-expanded')==='true';term.setAttribute('aria-expanded',String(!open));if(!open){definitionState.add(name.toLowerCase());safeSet('study-definition-opened',JSON.stringify([...definitionState]));updateDefinitionMission()}};term.addEventListener('click',toggle);term.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();toggle()}})});
  const glossary=q('#glossary');if(glossary&&!q('[data-definition-mission]')){const m=document.createElement('div');m.className='study-mini-mission';m.innerHTML='<div><strong>Mini mission · Developer-soup translator</strong><p>Open any 3 terms and read the fuller explanation + example.</p></div><div class="study-mini-progress" data-definition-mission>0 / 3 terms explored</div>';glossary.appendChild(m);updateDefinitionMission()}

  let drawer=q('.study-definition-drawer');if(!drawer){drawer=document.createElement('aside');drawer.className='study-definition-drawer';drawer.innerHTML='<header><h3>Definition</h3><button type="button" aria-label="Close definition">×</button></header><p></p><div class="example"></div>';document.body.appendChild(drawer);q('button',drawer).addEventListener('click',()=>drawer.classList.remove('open'))}
  const openDrawer=name=>{const d=lookup(name);if(!d)return;q('h3',drawer).textContent=name;q('p',drawer).textContent=d[0];q('.example',drawer).innerHTML=`<strong>Example:</strong> ${d[1]}`;drawer.classList.add('open');definitionState.add(name.toLowerCase());safeSet('study-definition-opened',JSON.stringify([...definitionState]));updateDefinitionMission()};
  qa('.future-tags span').forEach(chip=>{const name=chip.textContent.trim();if(!lookup(name))return;chip.classList.add('study-definition-chip');chip.tabIndex=0;chip.setAttribute('role','button');chip.setAttribute('aria-label',`Explain ${name}`);chip.addEventListener('click',()=>openDrawer(name));chip.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openDrawer(name)}})});

  const guideMap={
    '/study/':{
      foundation:['This is the four-layer anchor. Browser is where you touch the app; frontend controls the interface; backend handles private app rules; database keeps durable records.','Spaces example: open Personal Space in the browser → React shows the interface → backend supplies allowed data → PostgreSQL stores the durable records.'],
      react:['React is still frontend. The key idea is reusable components: known interface pieces that can receive different data and be assembled into different screens.','Spaces can reuse WeatherCard, CalendarCard, and AlertCard. The backend sends data/structure; React assembles the existing pieces.'],
      request:['A click does not teleport into Python. React handles the interface event, an HTTP request crosses to an API endpoint, backend logic runs, data may be read/saved, then a response comes back.','Create Briefing → React → POST request → FastAPI endpoint → Python logic → PostgreSQL → response → React updates the screen.'],
      backend:['Python is the language containing backend instructions. FastAPI is the framework handling web/API plumbing. PostgreSQL is storage. Keep those jobs separate.','FastAPI matches POST /briefings, Python decides how to create the briefing, PostgreSQL stores the durable result.'],
      database:['Rows are records, columns are fields, IDs identify records, and matching keys create relationships. The database stores; backend logic decides.','users.id = 1 and briefings.user_id = 1 connect Jay to his briefing.'],
      auth:['Authentication proves who you are. A session or token can carry continuing proof. Authorization then asks whether that known user is allowed to access this exact thing.','Jay can be logged in successfully and still receive 403 when asking for someone else’s private Space.'],
      boss:['This challenge makes the help fade. Rebuilding the path yourself proves you know the responsibilities instead of only recognizing a diagram.','Try saying the whole flow out loud before checking the answer.'],
      mastery:['Mastery here means “which mental models have you actually manipulated,” not a fake grade.','If Request Flow is weaker than React Pieces, replay the request animation and prediction chain.'],
      roadmap:['This is sequencing, not a pile of homework. The later words stay visible so they are familiar when their turn arrives.','You can know Docker exists today without needing container networking internals today.'],
      glossary:['Tap a term for a fuller explanation and an example. If one word is fuzzy, stop at that word instead of dragging five unknown words behind it.','Try endpoint, session, OpenAPI, or migration and compare the short definition to the expanded explanation.']
    },
    '/study/python/':{
      variables:['A variable is a useful name attached to a value. Python can use that name again later instead of hardcoding the same value everywhere.','briefing_title = "Morning Brief" gives the text a useful name the backend can reuse.'],
      decisions:['An if statement asks a true/false question, then Python follows the matching branch.','If urgency >= 7, place the item in priority; otherwise keep it normal.'],
      functions:['A function packages reusable instructions. Input goes in, the instructions run, and a result can come back.','make_greeting("Jay") runs the same instructions you could later run for another name.'],
      collections:['Lists hold ordered groups. Dictionaries hold labeled key/value pairs. They are two basic ways to organize multiple values.','A list can contain briefing card names; a dictionary can store title, visibility, and owner fields.'],
      fastapi:['FastAPI connects an HTTP method/path to Python code. The framework receives web requests; your Python function still contains the app-specific instructions.','GET /briefings/42 triggers a Python function that reads briefing 42 and returns structured data.'],
      boss:['This puzzle removes typing pressure. You are reasoning about code order and indentation before memorizing punctuation.','Define the function first, run indented instructions inside it, then return the result.'],
      mastery:['This map tracks the Python ideas you actually touched. Repeat a weak concept with different values instead of rereading the same paragraph.','Change the urgency, function input, and collections to prove the idea survives different examples.']
    },
    '/study/environment/':{
      overview:['Everything still revolves around browser → React frontend → FastAPI/Python backend → PostgreSQL. The surrounding tools help you build, verify, ship, protect, or recover that app.','Git records changes, Docker can package runtime, staging tests a release, and recovery gets you back when something breaks.'],
      frontend:['Frontend architecture is more than React. TypeScript adds checks, Router handles frontend locations, Query manages server data, and styling/UI tools help build the interface.','Personal Space can switch from Personal to Family while reusing the same component shapes and updating props/state.'],
      backend:['Separate the jobs: HTTP carries the message, method + path identify the endpoint, validation checks input, FastAPI routes it, Python logic decides, JSON carries structured results.','PATCH /briefings/42 can carry only changed settings, validate them, update the record, and return the new data.'],
      data:['PostgreSQL stores relational data. Queries read/write it, models describe data shapes, and migrations make schema changes explicit and reviewable.','Adding visibility to briefings should be a migration you can inspect, not a surprise production change.'],
      auth:['Security is a sequence of gates. First identity, then permission, then the protected resource.','Valid login + wrong resource permission = still denied.'],
      project:['The repository gives responsibilities predictable homes so generated changes do not become spaghetti.','Route receives the request, deeper logic/CRUD handles the work, models describe data, and the frontend lives separately under frontend/src.'],
      client:['FastAPI can describe its API through OpenAPI. The project generates matching TypeScript functions/types, and React calls those generated helpers.','Change an endpoint shape → regenerate the client → frontend gets updated typed API helpers.'],
      local:['Local development means several services can run on your own machine at once. Ports tell the browser which service you mean.','Frontend at localhost:5173, FastAPI at localhost:8000, interactive docs at localhost:8000/docs.'],
      workflow:['Software has a delivery lifecycle: build locally, validate, checkpoint in Git, test in staging, deploy a known revision, observe it, and recover if needed.','A Spaces backend change should be tested before it reaches real production data.'],
      guardrails:['AI assistance is useful because it can act, but that makes boundaries more important. Low-risk project edits can move quickly; large blast-radius actions should pause.','Creating a small route can be routine; changing production schema requires review; hardcoding a secret is never a silent shortcut.'],
      tools:['Tool names become easier when each gets one job: Git remembers changes, Docker repeats runtime, Spec Kit structures intent, Serena helps semantic code navigation, MCP connects approved capabilities.','Ask “what problem does this tool solve?” before learning commands.'],
      recovery:['Debugging starts with evidence, not random edits. Logs, diffs, migration state, and health checks answer different questions.','Server will not start? Read the first meaningful error before redesigning the architecture.'],
      boss:['This final path is the environment around the app: intent → plan → build → validate → Git → staging → production → observe → recover.','A successful deploy is not the end of the story. Observation and recovery are part of the system.']
    },
    '/study/environment/handbook/':{
      principles:['These principles explain why the environment exists: build real software while making the machinery visible enough that you become less dependent on generated magic over time.','A route or migration should arrive with an explanation of what changed, where it belongs, how it is checked, and how to recover.'],
      anatomy:['Project anatomy gives different responsibilities stable homes. Thin routes, testable behavior, explicit data shapes, tests, and configuration boundaries make changes easier to understand.','A request route should not secretly contain every database query, business rule, and deployment setting in one file.'],
      learning:['The learning loop is part of the environment: describe goal → map change → explain stack → build → validate → review diff.','When an endpoint is added, you should understand the endpoint, files touched, tests run, and Git diff, not only receive finished code.'],
      registry:['The question registry is intentionally unresolved in places. A good environment distinguishes decisions that are settled from details that still need a real answer.','“Which dependency manager?” stays an open question until the setup documents one choice and why.'],
      decisions:['Confirmed, proposed, and open are different confidence levels. Do not accidentally memorize a candidate tool as a permanent architectural law.','Python/FastAPI can be confirmed while an exact ORM choice is still open.'],
      acceptance:['The environment is finished only when workflows are repeatable and recoverable, not when the page looks polished.','You should be able to create a project, run it, migrate/backup data, validate changes, deploy safely, and recover using documented steps.']
    }
  };
  const routeGuide=guideMap[path]||{};
  let panel=q('#guidePanel'),launch=q('#guideLaunch'),guideText=q('#guideText');
  if(!panel){launch=document.createElement('button');launch.className='guide-launch';launch.id='guideLaunch';launch.type='button';launch.innerHTML='<span aria-hidden="true">✦</span><span>Ask Study Guide</span>';panel=document.createElement('aside');panel.className='guide-panel';panel.id='guidePanel';panel.innerHTML='<div class="guide-head"><strong>Study Guide</strong><button type="button" id="guideClose" aria-label="Close guide">×</button></div><div class="study-guide-context">Current section</div><p id="guideText"></p><div class="guide-actions"><button type="button" data-guide-mode="simple">Say it simpler</button><button type="button" data-guide-mode="why">Why do I care?</button><button type="button" data-guide-mode="example">Give me an example</button></div>';document.body.append(launch,panel);guideText=q('#guideText');q('#guideClose')?.addEventListener('click',()=>panel.classList.remove('open'));launch.addEventListener('click',()=>panel.classList.toggle('open'))}
  let contextEl=q('.study-guide-context',panel);if(!contextEl){contextEl=document.createElement('div');contextEl.className='study-guide-context';const head=q('.guide-head',panel);head?.insertAdjacentElement('afterend',contextEl)}
  let currentGuide={simple:'Pick a section and the guide will follow you.',why:'Each section has a different job in the learning path.',example:'Move through the course and watch this panel change.'};
  const setGuideSection=section=>{if(!section)return;const id=section.id||'overview',entry=routeGuide[id];const heading=q('h2,h3',section)?.textContent?.trim()||id;if(entry){currentGuide={simple:entry[0],example:entry[1],why:`Why this matters: ${entry[0]} Once this job is clear, the surrounding developer words stop blending together.`};if(guideText)guideText.textContent=currentGuide.simple}contextEl.textContent=`Current section · ${heading}`;const strong=q('.guide-head strong',panel);if(strong)strong.textContent=`Study Guide · ${heading}`;panel.classList.remove('study-guide-flash');void panel.offsetWidth;panel.classList.add('study-guide-flash')};
  qa('[data-guide-mode]',panel).forEach(btn=>btn.addEventListener('click',()=>{const mode=btn.dataset.guideMode;guideText.textContent=currentGuide[mode]||currentGuide.simple;panel.classList.add('open')}));
  const guideSections=qa('main section[id]').filter(s=>routeGuide[s.id]);if('IntersectionObserver'in window&&guideSections.length){const obs=new IntersectionObserver(es=>{const hit=es.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(hit)setGuideSection(hit.target)},{rootMargin:'-18% 0px -62% 0px',threshold:[0,.12,.35,.6]});guideSections.forEach(s=>obs.observe(s));setGuideSection(guideSections.find(s=>location.hash===`#${s.id}`)||guideSections[0])}

  const explainCodeLine=line=>{const s=line.trim();if(!s)return 'Blank space separates ideas so the code is easier to read.';if(/^@/.test(s))return 'Framework connection: this line connects an HTTP method/path to the Python function below.';if(/^def\s/.test(s)||/^async def\s/.test(s))return 'Function definition: Python is naming a reusable set of instructions and listing any inputs it expects.';if(/^if\s/.test(s))return 'Decision: Python checks whether this condition is true before choosing a branch.';if(/^else\s*:/.test(s))return 'Fallback branch: these instructions run when the earlier condition was false.';if(/^return\b/.test(s))return 'Return: send a result back to whatever called this function.';if(/\bawait\b/.test(s))return 'This waits for an async operation to finish without blocking the whole server process.';if(/^[\w.]+\s*=/.test(s))return 'Assignment: store a value under a useful name so later instructions can use it.';if(/\{.*\}/.test(s))return 'This creates or uses structured key/value data.';if(/\[.*\]/.test(s))return 'This creates or uses an ordered list of values.';if(/\.create\(|\.get\(|\.read\(|\.update\(|\.delete\(/.test(s))return 'This calls another reusable piece of logic instead of doing every job directly on this line.';return 'This is one instruction in the current block. Read what job it performs before worrying about punctuation.'};
  const codePlayCount=Number(safeGet('study-code-play-count')||0);
  let plays=codePlayCount;
  qa('pre').forEach(pre=>{if(pre.closest('.tree-panel,.tree'))return;if(pre.dataset.studyPlayer==='1')return;const raw=pre.textContent;if(!raw.trim()||raw.length>1800||raw.split('\n').length>22)return;pre.dataset.studyPlayer='1';pre.classList.add('study-code-scan');const controls=document.createElement('div');controls.className='study-code-controls';controls.innerHTML='<button class="study-code-play" type="button">▶ Animate this code</button><span class="study-code-explain">Watch one line at a time. Read the job first; syntax can come second.</span>';pre.insertAdjacentElement('afterend',controls);const play=q('.study-code-play',controls),explain=q('.study-code-explain',controls);let timer;play.addEventListener('click',()=>{clearInterval(timer);const text=pre.textContent,lines=text.split('\n');pre.textContent='';const spans=lines.map(line=>{const s=document.createElement('span');s.className='study-code-line';s.textContent=line||' ';pre.appendChild(s);return s});pre.classList.add('playing');let i=0;const show=()=>{spans.forEach((s,x)=>{s.classList.toggle('active',x===i);s.classList.toggle('done',x<i)});explain.textContent=explainCodeLine(lines[i]||'');if(i>=spans.length-1){clearInterval(timer);setTimeout(()=>pre.classList.remove('playing'),400);return}i++};show();timer=setInterval(show,900);plays++;safeSet('study-code-play-count',String(plays));if(plays===2&&!safeGet('study-code-badge')){safeSet('study-code-badge','1');toast('Code Tracer unlocked','you animated 2 code blocks')}})});
  q('#runFunction')?.addEventListener('click',()=>{const machine=q('.function-machine');if(!machine)return;machine.classList.remove('study-run');void machine.offsetWidth;machine.classList.add('study-run');setTimeout(()=>machine.classList.remove('study-run'),1900)});

  const wireProcess=root=>{const run=q('.study-process-run',root),steps=qa('.study-process-step',root),caption=q('.study-process-caption',root);if(!run||!steps.length)return;let timer;const show=i=>{steps.forEach((s,x)=>{s.classList.toggle('active',x===i);s.classList.toggle('done',x<i)});if(caption)caption.innerHTML=`<strong>${steps[i].querySelector('strong')?.textContent||''}</strong> · ${steps[i].dataset.explain||''}`};run.addEventListener('click',()=>{clearInterval(timer);let i=0;show(0);run.disabled=true;timer=setInterval(()=>{i++;if(i>=steps.length){clearInterval(timer);run.disabled=false;steps.forEach(s=>{s.classList.remove('active');s.classList.add('done')});return}show(i)},800)})};
  const processLab=(title,intro,steps,button='Animate process')=>`<div class="study-process-lab"><div class="study-process-head"><div><span class="study-term-hint">Animated process</span><h3>${title}</h3></div><p>${intro}</p><button class="study-process-run" type="button">${button}</button></div><div class="study-process-track" style="--process-cols:${steps.length}">${steps.map((s,i)=>`<article class="study-process-step" data-explain="${s[2]}"><span>${String(i+1).padStart(2,'0')}</span><strong>${s[0]}</strong><small>${s[1]}</small></article>`).join('')}</div><div class="study-process-caption">Press the button and follow one stage at a time.</div></div>`;

  const coverageItems=[
    ['Guides','Setup','How the project gets from repository to running development environment.'],['Guides','Spec Kit','Spec-driven development: intent → plan → tasks → implementation.'],['Guides','Serena','Semantic code navigation/editing for AI-assisted development.'],['Guides','Deploy to Render','How this application is released to the Render hosting platform.'],['Backend','Architecture','How request handling, logic, data access, and backend files divide responsibility.'],['Backend','Dependencies','Which Python packages the backend relies on and why repeatable versions matter.'],['Backend','Environment variables','Configuration and secrets supplied outside source code.'],['Backend','Database','PostgreSQL, models, relationships, queries, migrations, backup/recovery.'],['Frontend','Architecture','React + TypeScript interface structure and how UI calls the backend.'],['Frontend','Dependencies','React-side libraries such as routing, server-data, styling, and UI building blocks.'],['Frontend','Generated API client','FastAPI → OpenAPI → generated TypeScript functions/types → React.'],['Reference','Providers + libraries','The handbook reference index points to the third-party services and libraries the project touches.']
  ];
  const coverageHTML=()=>`<div class="study-handbook-coverage"><header><div><span class="study-term-hint">Handbook coverage map</span><h3>What the actual handbook says belongs in this environment</h3></div><p>Tap a card for the learning job. Exact provider-specific commands stay tied to the handbook instead of being guessed here.</p></header><div class="study-coverage-grid">${coverageItems.map((x,i)=>`<article class="study-coverage-card" tabindex="0" role="button" data-coverage="${i}"><span>${x[0]}</span><strong>${x[1]}</strong><small>${x[2]}</small></article>`).join('')}</div><div class="study-coverage-detail"><strong>Pick an area.</strong> This map is the checklist used to stop environment topics from disappearing behind generic web-development lessons.</div></div>`;
  const wireCoverage=root=>{const detail=q('.study-coverage-detail',root);qa('[data-coverage]',root).forEach(card=>{const show=()=>{const item=coverageItems[Number(card.dataset.coverage)];detail.innerHTML=`<strong>${item[0]} · ${item[1]}</strong><br>${item[2]} ${item[1]==='Deploy to Render'?'The handbook home explicitly includes a Render deployment guide; this course teaches the flow while the handbook remains the authority for exact service settings.':''}`};card.addEventListener('click',show);card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();show()}})})};

  if(path==='/study/environment/'||path==='/study/environment/handbook/'){
    const host=path==='/study/environment/'?q('#overview'):q('#principles');if(host&&!q('.study-handbook-coverage',host)){host.insertAdjacentHTML('beforeend',coverageHTML());wireCoverage(q('.study-handbook-coverage',host))}
  }
  if(path==='/study/environment/'){
    const local=q('#local');if(local&&!q('.study-process-lab[data-kind="setup"]',local)){const wrap=document.createElement('div');wrap.dataset.kind='setup';wrap.innerHTML=processLab('From repository to a running local app','The handbook has a Setup guide. This visual teaches the mental sequence without pretending a command is correct unless the handbook confirms it.',[
      ['Repository','Get the project files','The repository is the source of truth for the application code and documented setup.'],['Dependencies','Install what the project requires','Backend and frontend packages must be installed at the versions the project documents.'],['Environment','Supply configuration','Environment variables provide machine/environment-specific values without hardcoding secrets into source.'],['Services','Start required services','Backend, frontend, and PostgreSQL each need the services they depend on to be available.'],['Run locally','5173 + 8000','The known environment uses localhost:5173 for the frontend and localhost:8000 for FastAPI.'],['Inspect API','localhost:8000/docs','FastAPI interactive docs let you see and try the local backend endpoints.']],'Run setup story');local.appendChild(wrap.firstElementChild);q('.study-process-lab',local).dataset.kind='setup';wireProcess(q('.study-process-lab[data-kind="setup"]',local))}
    const workflow=q('#workflow');if(workflow&&!q('.study-process-lab[data-kind="render"]',workflow)){const holder=document.createElement('div');holder.innerHTML=processLab('What a Render deployment is doing','The handbook explicitly includes deploying to Render. This is the conceptual release flow; exact commands, service type, and environment settings should stay synchronized with that guide.',[
      ['Known Git revision','Choose what is being released','A deployment should point to reviewed source history instead of an unknown working directory.'],['Build','Install/build the application','Render prepares the service using the project’s configured build process.'],['Environment','Apply production configuration','Production environment variables and secrets belong outside public source code.'],['Start service','Run the backend/app','The hosting platform starts the configured application process.'],['Connect services','Database + dependencies','The running app must reach the production services it depends on.'],['Observe','Health + logs','Check whether the released revision actually starts and behaves correctly.'],['Recover','Rollback/fix if needed','A failed release needs a known recovery path instead of random production edits.']],'Animate deployment');const el=holder.firstElementChild;el.dataset.kind='render';workflow.appendChild(el);wireProcess(el)}
    const tools=q('#tools');if(tools&&!q('.study-process-lab[data-kind="deps"]',tools)){const holder=document.createElement('div');holder.innerHTML=processLab('Why dependencies need their own mental model','The handbook has backend and frontend dependency sections. A dependency is borrowed code your project relies on, so versions and repeatable installs matter.',[
      ['Your code','Application-specific behavior','This is the part you own and change for your product.'],['Dependency list','Declared external packages','The project records which outside packages it needs.'],['Version rules','Keep installs predictable','Version constraints reduce “works on one machine” surprises.'],['Install','Bring packages into the environment','The package manager resolves and installs the declared dependencies.'],['Import/use','Your code calls the library','FastAPI, React, TanStack, and other libraries become available to application code.'],['Update carefully','Review upgrades','Dependency upgrades can change behavior, so the environment treats them as a reviewable action.']],'Animate dependency flow');const el=holder.firstElementChild;el.dataset.kind='deps';tools.appendChild(el);wireProcess(el)}
  }

  qa('.study-process-lab').forEach(el=>{if(!el.dataset.wired){el.dataset.wired='1';wireProcess(el)}});
})();