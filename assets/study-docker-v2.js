(()=>{
  if(window.__studyDockerV2Loaded||location.pathname.replace(/\/+$/,'/')!=='/study/environment/')return;window.__studyDockerV2Loaded=true;
  const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];if(!q('link[href^="/assets/study-docker-v2.css"]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/assets/study-docker-v2.css?v=20260812-2';document.head.appendChild(l)}
  const host=q('#local');if(!host||q('.docker-v2',host))return;const box=document.createElement('div');box.className='docker-v2';box.innerHTML=`<div class="docker-v2-head"><div><span class="study-deep-tag">Docker · the version people should explain first</span><h3>Why Docker exists before we start naming its children</h3></div><p>The problem: your app expects a particular runtime and dependencies. Different machines can have different stuff installed. Docker makes that runtime more repeatable. Your database is a separate Aiven service, so do not picture Postgres trapped inside this Docker box.</p></div><div class="docker-v2-tabs"><button class="active" type="button" data-docker-v2="why">Why bother?</button><button type="button" data-docker-v2="image">Image</button><button type="button" data-docker-v2="container">Container</button><button type="button" data-docker-v2="compose">Compose</button><button type="button" data-docker-v2="actual">Your setup</button></div><div class="docker-v2-stage"><div class="docker-v2-copy"><h4 data-docker-title></h4><p data-docker-text></p><div class="analogy" data-docker-analogy></div></div><div class="docker-v2-visual" data-docker-visual></div></div>`;host.appendChild(box);
  const data={
    why:{title:'Without a repeatable runtime, “works on my laptop” becomes a personality disorder',text:'Your FastAPI app expects a Python runtime and a known package set. Another machine can have different versions or missing packages. Docker gives the app a repeatable runtime description. It does not magically turn every external service into a container.',analogy:'<b>Analogy:</b> instead of telling every kitchen “please own roughly the same ingredients and appliances,” ship a repeatable kitchen setup.'},
    image:{title:'Image = the packaged blueprint',text:'A Docker image is the prepared blueprint containing the application/runtime layers that a container can start from. It is not the running app yet.',analogy:'<b>Analogy:</b> image = a game installation/template. It is prepared, but nobody is playing it yet.'},
    container:{title:'Container = a running instance of that image',text:'A container is a running isolated process/environment started from an image. In your Render deployment, one Docker container runs the built application service.',analogy:'<b>Analogy:</b> image = cookie cutter; container = the actual cookie currently ruining your diet.'},
    compose:{title:'Compose = instructions for running related containerized services',text:'Docker Compose can describe how containerized services start and connect. Your base template includes Compose tooling, but the handbook is explicit that PostgreSQL itself lives remotely on Aiven, not in a local Postgres container.',analogy:'<b>Your setup:</b> do not memorize “Compose = frontend + backend + Postgres.” The database is an external managed service here.'},
    actual:{title:'Your actual handbook picture',text:'During development, React/Vite runs on port 5173 and FastAPI runs on port 8000. Both use the managed Aiven PostgreSQL service through environment settings. In Render production, the Dockerfile builds the frontend and places it inside the FastAPI image, so one Render Docker service serves the built frontend and API while Aiven remains the database.',analogy:'<b>Keep this picture:</b> local = two app dev servers + remote Aiven DB. Production = one Render app container + remote Aiven DB.'}
  };
  const visual={
    why:'<div class="docker-hosts"><div class="docker-host bad"><strong>Machine A 😌</strong><small>right Python + packages · app works</small></div><div class="docker-host bad"><strong>Machine B 💀</strong><small>different runtime · missing package · chaos</small></div></div>',
    image:'<div class="docker-layer-stack"><div class="docker-layer"><b>Docker image blueprint</b></div><div class="docker-layer">FastAPI application files</div><div class="docker-layer">Python runtime</div><div class="docker-layer">declared dependencies</div></div>',
    container:'<div class="docker-runtime"><div><strong>Image</strong><small>prepared blueprint</small></div><div class="active"><strong>Container</strong><small>running instance</small></div><div><strong>App process</strong><small>FastAPI serving requests</small></div></div>',
    compose:'<div class="docker-runtime"><div class="active"><strong>App service(s)</strong><small>container instructions</small></div><div><strong>Ports / env</strong><small>how they run/connect</small></div><div><strong>Aiven Postgres ↗</strong><small>remote managed DB, outside local container</small></div></div>',
    actual:'<div class="docker-hosts"><div class="docker-host good"><strong>Development</strong><small>Vite :5173 → FastAPI :8000 → Aiven Postgres ↗</small></div><div class="docker-host good"><strong>Render production</strong><small>Browser → one Docker app service (frontend + FastAPI) → Aiven Postgres ↗</small></div></div>'
  };
  const draw=k=>{qa('[data-docker-v2]',box).forEach(b=>b.classList.toggle('active',b.dataset.dockerV2===k));q('[data-docker-title]',box).textContent=data[k].title;q('[data-docker-text]',box).textContent=data[k].text;q('[data-docker-analogy]',box).innerHTML=data[k].analogy;q('[data-docker-visual]',box).innerHTML=visual[k]};qa('[data-docker-v2]',box).forEach(b=>b.addEventListener('click',()=>draw(b.dataset.dockerV2)));draw('why')})();