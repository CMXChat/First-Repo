(()=>{
  if(window.__studyAdvancedPeekLoaded)return;window.__studyAdvancedPeekLoaded=true;
  if(location.pathname.replace(/\/+$/,'/')!=='/study/environment/')return;
  const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];
  const host=q('#tools');if(!host||q('.advanced-peek',host))return;
  if(!q('link[href^="/assets/study-advanced-peek.css"]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/assets/study-advanced-peek.css?v=20260812-1';document.head.appendChild(l)}
  const groups={
    identity:{label:'Identity + browser proof',items:{
      cookie:{name:'Cookie',short:'browser-stored value sent with matching requests',simple:'A cookie is a small value the browser can automatically send back to a website. Apps often use cookies for login/session state, preferences or tracking.',why:'Without some continuing proof, you would have to log in again for every protected request.',here:'Your current handbook describes JWT auth and says the generated frontend client reads an auth token from localStorage. Cookie-based auth is a common alternative concept worth knowing, but it is not the specific mechanism described there.',now:'Know the idea. You do not need to redesign auth around cookies right now.'},
      token:{name:'Auth token',short:'proof carried with later protected requests',simple:'A token is a piece of data the client can present later to prove something, commonly that login already happened.',why:'The backend needs a way to recognize authenticated requests without receiving your password every time.',here:'The generated client in your handbook attaches the current auth token to API calls.',now:'Important now at the mental-model level. Exact security details can deepen later.'},
      jwt:{name:'JWT',short:'one structured token format',simple:'JWT stands for JSON Web Token. It is a signed token format containing claims such as who the user is and when the token expires.',why:'A server can verify that the token was issued by the app and has not been tampered with, using a secret/signing key.',here:'Your backend dependencies include pyjwt, and security.py handles JWT creation/verification.',now:'You should know what job it does. You do not need to memorize JWT internals or cryptography yet.'}
    }},
    data:{label:'Data plumbing',items:{
      pydantic:{name:'Pydantic',short:'validate typed Python data',simple:'Pydantic checks whether structured Python data matches the types and required fields you said it should have.',why:'Bad data should get rejected near the boundary instead of causing confusing bugs ten steps later.',here:'FastAPI uses Pydantic-style models for request/response validation in this stack.',now:'Worth learning soon because you will see validation errors constantly while building APIs.'},
      sqlmodel:{name:'SQLModel',short:'Python models that work with relational data',simple:'SQLModel lets typed Python classes describe app/database data while building on SQLAlchemy and Pydantic ideas.',why:'It reduces the gap between “what my Python object looks like” and “what my relational database table/record looks like.”',here:'Your handbook uses SQLModel for model classes and database work.',now:'Important, but learn tables/rows/IDs first so the abstraction has something to attach to.'},
      orm:{name:'ORM',short:'maps code objects to relational database work',simple:'ORM means Object-Relational Mapper. It lets code work with model objects while the ORM generates a lot of the SQL underneath.',why:'Developers often prefer typed code APIs over writing raw SQL for every common operation.',here:'SQLModel is the ORM-style layer in your current backend stack.',now:'Understand the translator idea. Raw SQL still matters because it explains what the database ultimately does.'},
      driver:{name:'psycopg',short:'actual PostgreSQL driver underneath',simple:'A database driver is the low-level library that knows how to connect to PostgreSQL and speak its protocol from Python.',why:'An ORM can describe a query, but something still has to open the connection and send database commands.',here:'Your handbook names psycopg as the actual PostgreSQL driver under SQLModel.',now:'Awareness is enough until connection errors make you care more 😭.'},
      alembic:{name:'Alembic',short:'versioned database schema changes',simple:'Alembic records changes to your database structure as migrations, like adding a column or creating a table.',why:'Every environment needs to change its schema in the same deliberate order instead of someone manually clicking around a database.',here:'Your handbook uses Alembic and runs alembic upgrade head during startup/deploy preparation.',now:'Learn this before you start making real schema changes.'}
    }},
    shipping:{label:'Build + ship',items:{
      vite:{name:'Vite',short:'frontend dev server + build tool',simple:'Vite helps run your React frontend quickly during development and turns the source into optimized files for production.',why:'Browsers do not directly run your whole TypeScript/React source setup exactly as written. A build tool handles transformation and bundling.',here:'Your frontend handbook lists Vite as the development/build tool.',now:'Know dev server versus production build. Deep bundler internals can stay cursed and distant.'},
      image:{name:'Docker image',short:'packaged read-only runtime blueprint',simple:'A Docker image is the prepared package describing the files, runtime and dependencies a container should start from.',why:'You want the same app/runtime recipe to start consistently on another machine or hosting platform.',here:'The Render deployment builds backend/Dockerfile, which also builds/copies the frontend into the final app image.',now:'Important enough to understand now because deployment uses it.'},
      container:{name:'Docker container',short:'running instance of an image',simple:'A container is what you get when an image is actually started and running.',why:'The image is the package; the container is the live process/environment using that package.',here:'Render starts the app container built from your Dockerfile.',now:'Know image versus container clearly. That one distinction prevents a lot of Docker confusion.'},
      ci:{name:'CI / GitHub Actions',short:'automatic checks on code changes',simple:'Continuous Integration means robots run checks such as syntax, tests or validation whenever code changes.',why:'Humans forget steps. CI turns important checks into repeatable evidence before changes are trusted.',here:'This repo now literally has Study Lab QA running in GitHub Actions.',now:'Very useful now because you can watch your own changes pass or fail instead of treating CI as theory.'}
    }},
    network:{label:'Networking deeper',items:{
      origin:{name:'Origin',short:'scheme + host + port identity',simple:'For browser security, an origin is basically protocol + hostname + port together. http://localhost:5173 and http://localhost:8000 are different origins.',why:'Browsers use origins when deciding whether a page is making a cross-origin request.',here:'During development your frontend and backend run on different ports, which is why CORS configuration matters.',now:'Worth knowing once localhost ports stop feeling mysterious.'},
      cors:{name:'CORS',short:'browser permission rules for cross-origin calls',simple:'CORS is how a backend tells browsers which other origins are allowed to make certain requests to it.',why:'Without browser origin rules, one random site could try to use your logged-in browser to call another site’s API.',here:'Your handbook has BACKEND_CORS_ORIGINS and automatically allows FRONTEND_HOST.',now:'Learn the mental model now. The header details can wait until you hit a real CORS error.'},
      tls:{name:'TLS / HTTPS',short:'encrypt web traffic in transit',simple:'TLS is the encryption layer underneath HTTPS. It protects traffic while it travels between client and server.',why:'Passwords, tokens and private app data should not travel across networks as readable plain text.',here:'Aiven requires encrypted database connections too; your web deployment should also use HTTPS.',now:'Know the security job. You do not need to study cipher suites unless suffering becomes a hobby.'},
      proxy:{name:'Reverse proxy',short:'front door that routes traffic to app services',simple:'A reverse proxy sits in front of backend services and forwards incoming requests to the right place, often handling HTTPS or routing.',why:'Large deployments may have several internal services but want one clean public entry point.',here:'The current Render handbook deliberately uses one service and says no separate Traefik setup is needed for that deployment path.',now:'General concept only. Do not add one just because architecture diagrams look sexier with more boxes.'}
    }},
    later:{label:'Later powers',items:{
      redis:{name:'Redis',short:'very fast in-memory key/value data service',simple:'Redis keeps data in memory for extremely fast access and is often used for caching, temporary state, queues or background-job coordination.',why:'Some workloads need faster/shorter-lived data than repeatedly querying the main relational database.',here:'Your handbook explicitly says Redis is not wired into the app yet. If a future feature needs it, Aiven is the planned service source.',now:'Awareness only. Do not add Redis because a YouTube architecture guy used it.'},
      cache:{name:'Cache',short:'temporary copy of expensive-to-get data',simple:'A cache keeps a convenient temporary copy so the app can avoid doing the same slow work repeatedly.',why:'Repeated database queries, API calls or computation can be expensive or slow.',here:'TanStack Query already gives the frontend a form of server-data caching. Backend caching could later use something such as Redis.',now:'Know the concept. Add caching when measurements show repeated work actually needs it.'},
      jobs:{name:'Background job',short:'work that does not need to finish inside one live request',simple:'A background job lets the system do work later or separately, such as generating a morning briefing before the user opens the app.',why:'Long or scheduled tasks should not force a browser request to sit open waiting forever.',here:'Your Spaces morning-briefing idea is a perfect future example even though the base handbook does not wire a queue system yet.',now:'Conceptually useful now. Implementation can wait until you actually build scheduled work.'},
      websocket:{name:'WebSocket',short:'long-lived two-way browser/server connection',simple:'WebSockets keep a connection open so browser and server can send messages to each other without starting a fresh HTTP request for every update.',why:'Useful for chat, live presence, realtime dashboards and other push-heavy experiences.',here:'Not part of the core handbook flow you are learning right now. Normal HTTP request/response should stay your foundation first.',now:'Peek only. Learn it when a feature genuinely needs realtime behavior.'}
    }}
  };
  const lab=document.createElement('div');lab.className='advanced-peek';lab.innerHTML=`<div class="advanced-peek-head"><div><span class="study-deep-tag">optional rabbit hole · the stuff we were saving for later</span><h3>Developer Soup After Dark 🌚</h3></div><p>These words matter eventually, but you are allowed to learn the job before the internals. Pick a group, then a term. Every card tells you what it is, why anyone invented it, how it relates to this environment and whether you should care today.</p></div><div class="advanced-peek-groups">${Object.entries(groups).map(([k,g],i)=>`<button class="${i===0?'active':''}" type="button" data-adv-group="${k}">${g.label}</button>`).join('')}</div><div class="advanced-peek-stage"><div class="advanced-term-list" data-adv-list></div><div class="advanced-term-detail" data-adv-detail></div></div>`;
  host.appendChild(lab);let group=Object.keys(groups)[0],term=Object.keys(groups[group].items)[0];
  const renderList=()=>{const g=groups[group];q('[data-adv-list]',lab).innerHTML=Object.entries(g.items).map(([k,v])=>`<button class="${k===term?'active':''}" type="button" data-adv-term="${k}"><strong>${v.name}</strong><small>${v.short}</small></button>`).join('');qa('[data-adv-term]',lab).forEach(b=>b.addEventListener('click',()=>{term=b.dataset.advTerm;renderList();renderDetail()}))};
  const renderDetail=()=>{const v=groups[group].items[term];q('[data-adv-detail]',lab).innerHTML=`<span class="tag">${groups[group].label}</span><h4>${v.name}</h4><p>${v.simple}</p><div class="advanced-facts"><div class="advanced-fact"><span>Why it exists</span><p>${v.why}</p></div><div class="advanced-fact"><span>In your environment</span><p>${v.here}</p></div><div class="advanced-fact"><span>Do you need it now?</span><p><b>${v.now}</b></p></div><div class="advanced-fact"><span>Say it out loud</span><p>“${v.name} = ${v.short}.”</p></div></div>`};
  qa('[data-adv-group]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-adv-group]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');group=b.dataset.advGroup;term=Object.keys(groups[group].items)[0];renderList();renderDetail()}));renderList();renderDetail();
})();