(()=>{
  if(window.__studyHandbookSyncLoaded||location.pathname.replace(/\/+$/,'/')!=='/study/environment/')return;window.__studyHandbookSyncLoaded=true;
  const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];
  if(!q('link[href^="/assets/study-handbook-sync.css"]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/assets/study-handbook-sync.css?v=20260812-1';document.head.appendChild(l)}
  const mount=(host,html)=>{if(!host)return null;const w=document.createElement('div');w.innerHTML=html.trim();const el=w.firstElementChild;host.appendChild(el);return el};
  const animate=(nodes,caption,copy,ms=760)=>{let i=0,t;const show=()=>{nodes.forEach((n,x)=>n.classList.toggle('active',x===i));if(copy[i])caption.innerHTML=`<b>${copy[i][0]}:</b> ${copy[i][1]}`};show();t=setInterval(()=>{i++;if(i>=nodes.length){clearInterval(t);nodes.forEach(n=>n.classList.remove('active'));return}show()},ms);return()=>clearInterval(t)};

  const overview=q('#overview');if(overview&&!q('[data-hb-reality]',overview)){const lab=mount(overview,`<div class="handbook-sync" data-hb-reality><div class="handbook-sync-head"><div><span class="hb-kicker">actual handbook stack · no generic tutorial fanfic</span><h3>Same app, two very different runtime pictures</h3></div><p>Development has two separate app servers. Production on Render is one Docker service. PostgreSQL stays on managed Aiven in both pictures.</p></div><div class="hb-tabs"><button class="active" type="button" data-hb-stack="dev">💻 Codespaces / dev</button><button type="button" data-hb-stack="prod">🚀 Render / production</button></div><div class="hb-stack-map"></div><div class="hb-stack-caption"><b>Development:</b> browser → Vite/React :5173 → FastAPI :8000 → Aiven PostgreSQL.</div><div class="study-deep-actions"><button class="primary" type="button" data-hb-stack-run>▶ Animate actual stack</button></div></div>`);let mode='dev',stop;const maps={dev:[['Browser','what you open','React UI appears here'],['Vite :5173','frontend dev server','serves the React app'],['HTTP/API','VITE_API_URL','points React at backend'],['FastAPI :8000','Python backend','routes + logic + auth'],['Aiven Postgres ↗','managed database','remote durable records']],prod:[['Browser','public URL','loads built frontend'],['Render','one service','created from render.yaml'],['Docker container','frontend + FastAPI','frontend built into backend image'],['FastAPI API','same app service','serves UI + API'],['Aiven Postgres ↗','managed database','still external'] ]};const copy={dev:[['Browser','You open the forwarded frontend URL in Codespaces.'],['Vite','The React development server runs separately on port 5173.'],['API URL','VITE_API_URL tells the frontend where the separately running backend lives.'],['FastAPI','Backend listens on port 8000 and handles API/auth/Python work.'],['Aiven','Database traffic goes to the managed Postgres service. Nothing here spins up local Postgres.']],prod:[['Browser','The user opens the deployed Render URL.'],['Render','Render reads the Blueprint and runs one application service.'],['Docker','The Dockerfile has already built the frontend and copied it into the FastAPI image.'],['FastAPI','One running service serves the built frontend and API.'],['Aiven','Postgres stays managed outside that container.']]};const draw=()=>{q('.hb-stack-map',lab).innerHTML=maps[mode].map((x,i)=>`<article class="hb-stack-node"><span>${String(i+1).padStart(2,'0')} · ${x[1]}</span><strong>${x[0]}</strong><small>${x[2]}</small></article>`).join('');q('.hb-stack-caption',lab).innerHTML=mode==='dev'?'<b>Keep this:</b> Vite 5173 + FastAPI 8000 are separate in development; Aiven is remote.':'<b>Keep this:</b> Render runs one Docker application service containing the built frontend + FastAPI; Aiven is still remote.'};qa('[data-hb-stack]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-hb-stack]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');mode=b.dataset.hbStack;stop?.();draw()}));q('[data-hb-stack-run]',lab).addEventListener('click',()=>{stop?.();stop=animate(qa('.hb-stack-node',lab),q('.hb-stack-caption',lab),copy[mode])});draw()}

  const project=q('#project');if(project&&!q('[data-hb-files]',project)){const lab=mount(project,`<div class="handbook-sync" data-hb-files><div class="handbook-sync-head"><div><span class="hb-kicker">backend architecture · exact handbook chain</span><h3>Model → CRUD → Route, with deps wiring shared plumbing</h3></div><p>The filename matters less once you know the job. Tap each card and attach the file to a responsibility.</p></div><div class="hb-file-flow"><button class="hb-file-card" type="button" data-hb-file="model"><code>app/models.py</code><strong>1. Model</strong><small>DB tables + Create/Update/Public shapes</small></button><button class="hb-file-card" type="button" data-hb-file="crud"><code>app/crud.py</code><strong>2. CRUD</strong><small>actual database operations routes call</small></button><button class="hb-file-card" type="button" data-hb-file="route"><code>app/api/routes/</code><strong>3. Route</strong><small>exposes the feature over HTTP</small></button><button class="hb-file-card" type="button" data-hb-file="deps"><code>app/api/deps.py</code><strong>+ deps</strong><small>current user + DB session plumbing</small></button></div><div class="hb-detail"><b>Try a card:</b> the chain becomes less scary when every file has one boring job.</div><div class="hb-library-cloud" style="margin-top:10px"><button class="hb-library" data-core-file="main">app/main.py</button><button class="hb-library" data-core-file="config">core/config.py</button><button class="hb-library" data-core-file="db">core/db.py</button><button class="hb-library" data-core-file="security">core/security.py</button><button class="hb-library" data-core-file="api">api/main.py</button><button class="hb-library" data-core-file="migrations">alembic/versions/</button><button class="hb-library" data-core-file="prestart">backend_pre_start.py</button></div><div class="hb-library-detail"><b>Extra files:</b> tap one to see why it exists.</div></div>`);const detail=q('.hb-detail',lab),lib=q('.hb-library-detail',lab);const f={model:'A model defines the shape of data. In this handbook, SQLModel classes cover database tables plus Create/Update/Public shapes.',crud:'CRUD functions perform reusable database operations. A route can call them instead of stuffing SQL/database work directly into the HTTP handler.',route:'A route is the HTTP-facing door. login.py handles auth, users.py handles users, items.py is the example feature pattern, private.py is superuser-only, utils.py includes health/test-email.',deps:'deps.py injects shared request plumbing such as get_current_user and a database session into routes.'};qa('[data-hb-file]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-hb-file]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');detail.innerHTML=`<b>${b.querySelector('strong').textContent}:</b> ${f[b.dataset.hbFile]}`}));const core={main:'Creates the FastAPI app, configures CORS, and mounts the rest of the application.',config:'Reads settings such as secrets, environment, Postgres and email configuration.',db:'Creates the database engine/session setup used by backend code.',security:'Owns password hashing plus JWT creation/verification in this template.',api:'Stitches the route modules together under /api/v1.',migrations:'One Alembic file per database schema change, applied in order.',prestart:'Waits for the managed database to be reachable before the app starts.'};qa('[data-core-file]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-core-file]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');lib.innerHTML=`<b>${b.textContent}:</b> ${core[b.dataset.coreFile]}`}))}

  const backend=q('#backend');if(backend&&!q('[data-hb-backend-deps]',backend)){const deps={fastapi:['Web framework','Turns Python functions into HTTP endpoints.'],pydantic:['Validation','Checks request/response data against expected shapes.'],sqlmodel:['ORM/model layer','Combines model validation ideas with relational database mapping.'],psycopg:['Postgres driver','The lower-level driver actually speaking PostgreSQL underneath SQLModel.'],alembic:['Migrations','Creates/runs ordered database schema changes.'],pyjwt:['JWT auth','Creates/verifies the JWT tokens used for login sessions in this template.'],pwdlib:['Password hashing','Argon2/bcrypt hashing so readable passwords are not stored.'],tenacity:['Retry logic','Used while waiting for the database to be ready during startup.'],sentry:['Error tracking','Optional; inactive unless SENTRY_DSN is configured.'],pytest:['Tests','Runs backend tests; coverage measures what those tests exercise.'],ruff:['Lint + format','Checks/auto-formats Python code.'],types:['Type checking','mypy + ty help catch type problems.']};const lab=mount(backend,`<div class="handbook-sync" data-hb-backend-deps><div class="handbook-sync-head"><div><span class="hb-kicker">backend packages · attach names to problems</span><h3>What do I blame when this thing breaks?</h3></div><p>You do not need package trivia. You need enough recognition to know which layer deserves investigation.</p></div><div class="hb-blame-grid">${Object.entries(deps).map(([k,v])=>`<button class="hb-blame" type="button" data-blame="${k}"><span>${v[0]}</span><strong>${k==='sentry'?'sentry-sdk':k==='types'?'mypy + ty':k}</strong><small>${v[1]}</small></button>`).join('')}</div><div class="hb-detail"><b>Example:</b> “field required” points toward Pydantic/models. Migration won’t apply? Alembic. Auth token/password issue? JWT/pwdlib/security.py.</div></div>`);qa('[data-blame]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-blame]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');const [job,desc]=deps[b.dataset.blame];q('.hb-detail',lab).innerHTML=`<b>${b.querySelector('strong').textContent} · ${job}:</b> ${desc}`}))}

  const data=q('#data');if(data&&!q('[data-hb-aiven]',data)){mount(data,`<div class="handbook-sync" data-hb-aiven><div class="handbook-sync-head"><div><span class="hb-kicker">database reality check</span><h3>PostgreSQL is the database. Aiven is where this Postgres lives.</h3></div><p>Those are different jobs. PostgreSQL is the database technology; Aiven is the managed hosting service running it for this environment.</p></div><div class="hb-stack-map"><article class="hb-stack-node"><span>app</span><strong>FastAPI</strong><small>asks for database work</small></article><article class="hb-stack-node"><span>ORM</span><strong>SQLModel</strong><small>maps Python models/operations</small></article><article class="hb-stack-node"><span>driver</span><strong>psycopg</strong><small>speaks PostgreSQL protocol</small></article><article class="hb-stack-node"><span>database</span><strong>PostgreSQL</strong><small>tables, rows, relations</small></article><article class="hb-stack-node"><span>host</span><strong>Aiven ↗</strong><small>managed remote service</small></article></div><div class="hb-detail"><b>Secrets:</b> host, port, database, user and password map to POSTGRES_SERVER, POSTGRES_PORT, POSTGRES_DB, POSTGRES_USER and POSTGRES_PASSWORD. The handbook keeps real credentials in Codespaces secrets.</div></div>`)}

  const frontend=q('#frontend');if(frontend&&!q('[data-hb-frontend]',frontend)){const libs={react:['UI framework','components + interactive UI'],vite:['Dev/build tool','serves/builds the frontend'],router:['TanStack Router','file-based routing; route file path becomes URL'],query:['TanStack Query','fetches, caches and re-fetches server data'],axios:['HTTP client','sits underneath generated API client'],tailwind:['Tailwind','utility styling'],shadcn:['shadcn/ui + Radix','reusable accessible UI primitives/patterns'],forms:['react-hook-form + Zod','form state/submission + validation'],biome:['Biome','lint + format; this project does not use ESLint/Prettier'],playwright:['Playwright','end-to-end browser testing']};const lab=mount(frontend,`<div class="handbook-sync" data-hb-frontend><div class="handbook-sync-head"><div><span class="hb-kicker">frontend handbook chain</span><h3>Route → generated Service → TanStack Query → component</h3></div><p>Same trick as backend architecture: follow the chain instead of memorizing the whole repo.</p></div><div class="hb-client-flow"><article class="hb-client-node"><span>01</span><strong>src/routes/</strong><small>page / URL</small></article><article class="hb-client-node"><span>02</span><strong>*Service()</strong><small>generated API helper</small></article><article class="hb-client-node"><span>03</span><strong>TanStack Query</strong><small>fetch/cache/re-fetch</small></article><article class="hb-client-node"><span>04</span><strong>components/Feature/</strong><small>feature UI pieces</small></article><article class="hb-client-node"><span>05</span><strong>browser</strong><small>rendered interface</small></article></div><div class="hb-library-cloud" style="margin-top:13px">${Object.entries(libs).map(([k,v])=>`<button class="hb-library" type="button" data-front-lib="${k}">${v[0]}</button>`).join('')}</div><div class="hb-library-detail"><b>Tap a frontend tool:</b> learn its job, not its résumé.</div></div>`);let stop;const nodes=qa('.hb-client-node',lab);nodes[0]?.parentElement.insertAdjacentHTML('afterend','<div class="study-deep-actions" style="margin-top:10px"><button class="primary" type="button" data-front-run>▶ Animate new page</button></div>');q('[data-front-run]',lab)?.addEventListener('click',()=>{stop?.();stop=animate(nodes,q('.hb-library-detail',lab),[['Route file','TanStack Router chooses the page from src/routes/.'],['Generated Service','The page calls a generated API helper that matches the backend contract.'],['Query','TanStack Query manages loading/caching/refetching around that call.'],['Components','Feature components turn returned data into reusable interface pieces.'],['Browser','React renders the result you can actually use.']])});qa('[data-front-lib]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-front-lib]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');const [name,job]=libs[b.dataset.frontLib];q('.hb-library-detail',lab).innerHTML=`<b>${name}:</b> ${job}.`}))}

  const client=q('#client');if(client&&!q('[data-hb-client]',client)){const lab=mount(client,`<div class="handbook-sync" data-hb-client><div class="handbook-sync-head"><div><span class="hb-kicker">generated API client · exact handbook rule</span><h3>Change backend → OpenAPI changes → regenerate frontend helpers</h3></div><p>Everything under frontend/src/client/ is generated. Fix the backend contract, regenerate, then let React call the generated Service functions.</p></div><div class="hb-client-flow"><article class="hb-client-node"><span>01</span><strong>FastAPI route/model</strong><small>backend contract changes</small></article><article class="hb-client-node"><span>02</span><strong>OpenAPI</strong><small>machine-readable API description</small></article><article class="hb-client-node"><span>03</span><strong>generate-client</strong><small>@hey-api/openapi-ts builds helpers</small></article><article class="hb-client-node"><span>04</span><strong>src/client/</strong><small>generated types + *Service</small></article><article class="hb-client-node"><span>05</span><strong>React + Query</strong><small>calls generated helper</small></article></div><pre class="hb-command"># from repo root\nbun run generate-client</pre><div class="hb-warning"><b>Do not hand-edit frontend/src/client/.</b> Regeneration overwrites it. The handbook says a stale generated client is a common reason frontend types stop matching a changed backend.</div><div class="study-deep-actions"><button class="primary" type="button" data-client-run>▶ Animate regeneration</button></div><div class="hb-detail"><b>Example:</b> add a field/endpoint in FastAPI → regenerate client → React sees matching TypeScript types/functions.</div></div>`);let stop;q('[data-client-run]',lab).addEventListener('click',()=>{stop?.();stop=animate(qa('.hb-client-node',lab),q('.hb-detail',lab),[['Backend','You change a route, model or response shape.'],['OpenAPI','FastAPI exports an updated description of that API.'],['Generator','bun run generate-client rebuilds the frontend client from that description.'],['Generated code','src/client/ now matches the backend. Do not edit these generated files manually.'],['Frontend','TanStack Query/React call the matching *Service function.']])})}

  const local=q('#local');if(local&&!q('[data-hb-setup]',local)){const steps=[['01','Set Codespaces secrets','Postgres + SECRET_KEY + first superuser','before codespace creation'],['02','Create Codespace','devcontainer installs uv, bun, AI CLIs','automatic setup'],['03','Run first migration','create tables in new Aiven DB','cd backend && uv run alembic upgrade head'],['04','Set VITE_API_URL','point frontend :5173 at backend :8000','frontend/.env'],['05','Start FastAPI','backend dev server','cd backend && fastapi run --reload app/main.py'],['06','Start Vite','frontend dev server','bun run dev'],['07','Log in','first superuser created at startup','use configured secrets'],['08','Build with AI','Spec Kit + Serena already wired','small reviewed changes']];const lab=mount(local,`<div class="handbook-sync" data-hb-setup><div class="handbook-sync-head"><div><span class="hb-kicker">getting started · the actual mechanical path</span><h3>From repo to running full-stack app</h3></div><p>Click through the exact handbook sequence. This is the boring part that turns “GitHub page” into frontend + backend + database.</p></div><div class="hb-setup-list">${steps.map(x=>`<article class="hb-setup-step"><span>${x[0]}</span><div><strong>${x[1]}</strong><small>${x[2]}</small></div><code>${x[3]}</code></article>`).join('')}</div><div class="study-deep-actions"><button class="primary" type="button" data-setup-run>▶ Walk me through it</button></div><div class="hb-setup-caption"><b>Start:</b> secrets first. Codespaces only injects them when the environment starts.</div></div>`);let stop;q('[data-setup-run]',lab).addEventListener('click',()=>{stop?.();stop=animate(qa('.hb-setup-step',lab),q('.hb-setup-caption',lab),steps.map(x=>[x[1],`${x[2]}. ${x[3]}`]),850)})}

  const workflow=q('#workflow');if(workflow&&!q('[data-hb-render]',workflow)){const lab=mount(workflow,`<div class="handbook-sync" data-hb-render><div class="handbook-sync-head"><div><span class="hb-kicker">Render deployment · actual handbook shape</span><h3>GitHub → render.yaml → one Docker service → Aiven</h3></div><p>The production deployment is intentionally simpler than the local dev picture: one Render service runs the Docker image containing the built frontend + FastAPI backend.</p></div><div class="hb-stack-map"><article class="hb-stack-node"><span>01</span><strong>Git push</strong><small>connected branch</small></article><article class="hb-stack-node"><span>02</span><strong>render.yaml</strong><small>Blueprint defines service</small></article><article class="hb-stack-node"><span>03</span><strong>Docker build</strong><small>frontend built into FastAPI image</small></article><article class="hb-stack-node"><span>04</span><strong>prestart</strong><small>migrations + initial user setup</small></article><article class="hb-stack-node"><span>05</span><strong>Render service ↔ Aiven</strong><small>live app + remote DB</small></article></div><div class="study-deep-actions"><button class="primary" data-render-run type="button">▶ Deploy the pretend app</button></div><div class="hb-stack-caption"><b>Secrets stay out of render.yaml:</b> actual values are configured in Render’s Environment settings.</div></div>`);let stop;q('[data-render-run]',lab).addEventListener('click',()=>{stop?.();stop=animate(qa('.hb-stack-node',lab),q('.hb-stack-caption',lab),[['Git','A reviewed revision reaches the branch Render watches.'],['Blueprint','Render reads render.yaml instead of making you click-configure the whole service by hand.'],['Build','backend/Dockerfile builds the frontend and copies it into the FastAPI image.'],['Prestart','Startup runs migrations and initial data setup before serving traffic.'],['Live','One Render app service serves the frontend/API and connects to Aiven Postgres.']])})}

  const tools=q('#tools');if(tools&&!q('[data-hb-tools]',tools)){const lab=mount(tools,`<div class="handbook-sync" data-hb-tools><div class="handbook-sync-head"><div><span class="hb-kicker">AI coding workflow · exact handbook</span><h3>Spec Kit tells AI what to build. Serena helps it find the right code.</h3></div><p>Neither tool replaces understanding. The handbook still says: small increments, explain changes back, restrictive permissions first, and read the diff like a coworker’s PR.</p></div><div class="hb-tabs"><button class="active" data-hb-tool="spec" type="button">Spec Kit</button><button data-hb-tool="serena" type="button">Serena</button></div><div class="hb-tool-flow"></div><div class="hb-detail"></div><div class="study-deep-actions"><button class="primary" type="button" data-tool-run>▶ Animate workflow</button></div></div>`);let mode='spec',stop;const flows={spec:[['Constitution','set project rules once'],['Specify','describe feature in plain English'],['Plan','technical approach'],['Tasks','small implementation steps'],['Implement','AI writes code + you review']],serena:[['Your request','multi-file change'],['Semantic map','functions/classes/usages'],['Find impact','every relevant usage'],['Bounded edit','change right places'],['Review diff','you still decide if it is correct']]};const details={spec:'<b>Starter sequence:</b> constitution once; then specify → plan → tasks → implement. The handbook says the extra clarify/checklist/analyze/converge commands can wait.',serena:'<b>Serena:</b> already wired into the project. Useful for “find every usage,” rename across files, or model changes that touch multiple places. First use may write .serena/memories/. Avoid two AI CLIs working the same folder simultaneously because their Serena notes can overwrite each other.'};const draw=()=>{q('.hb-tool-flow',lab).innerHTML=flows[mode].map((x,i)=>`<article class="hb-tool-step"><span>${String(i+1).padStart(2,'0')}</span><strong>${x[0]}</strong><small>${x[1]}</small></article>`).join('');q('.hb-detail',lab).innerHTML=details[mode]};qa('[data-hb-tool]',lab).forEach(b=>b.addEventListener('click',()=>{qa('[data-hb-tool]',lab).forEach(x=>x.classList.remove('active'));b.classList.add('active');mode=b.dataset.hbTool;stop?.();draw()}));q('[data-tool-run]',lab).addEventListener('click',()=>{stop?.();stop=animate(qa('.hb-tool-step',lab),q('.hb-detail',lab),flows[mode].map(x=>x),820)});draw()}
})();