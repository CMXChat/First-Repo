name: Brief Browser Matrix

on:
  pull_request:
    paths:
      - 'brief/**'
      - 'assets/brief/**'
      - 'assets/daily-video.js'
      - 'tests/brief-finalization-e2e.spec.cjs'
      - 'tests/brief-theme-e2e.spec.cjs'
      - 'tests/playwright.config.cjs'
      - '.github/workflows/brief-browser-matrix.yml'

permissions:
  contents: read

jobs:
  browser-matrix:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Playwright test runner
        run: npm install --no-save @playwright/test@1.55.0

      - name: Install browser engines
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Run final product and theme flows in every browser project
        run: npx playwright test tests/brief-finalization-e2e.spec.cjs tests/brief-theme-e2e.spec.cjs --config=tests/playwright.config.cjs

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: brief-final-browser-results
          path: test-results
          if-no-files-found: ignore
          retention-days: 7
